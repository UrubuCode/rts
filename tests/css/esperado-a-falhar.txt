# Fixtures do corpus CSS que FALHAM de proposito: cada uma nomeia uma lacuna do
# motor medida no Blink (tests/css/README.md, "O numero, hoje"). O job dom-rulers
# do CI fica vermelho se falhar uma fixture que NAO esta aqui, e avisa se uma
# daqui passar (a lista ficou para tras). Uma linha por ficheiro; # e comentario.

# folha de UA (lote I): largura de texto a negrito e de controlos no medidor
# aproximado, fonte dos controlos, <tr> sem border-spacing horizontal
claude-ua-form-disabled.html
claude-ua-headings.html
claude-ua-th.html

# cursor: url(x.png) — o Blink resolve a URL contra a base do documento;
# nenhuma propriedade deste motor resolve URLs (lote S-decor, dito no teste)
claude-cursor-pointer-events.html

# vaga 6 — as três fixtures medidas ANTES do código (`hyphens-manual`,
# `inline-fragmentos`, `font-unidades-ch-ex`) saíram daqui com os lotes S-hifen,
# S-inline-2 e T.

# vaga 7 — `claude-img-natural` (medida antes do código) saiu daqui com o lote
# V-img: o PNG de `data:` chega ao `<img>` pela ponte.

# lote imagens-no-raster — a fonte é `data:image/svg+xml`; o motor só
# descodifica PNG (`rts_dom::imagem`, PLAN.md lote V-img) e por isso o <img>
# nunca tem razão de aspeto para transferir, mesmo com o stretch de flex a
# funcionar (corpus Rust: `layout::tests::imagens_corpus`). Corte documentado,
# não deste lote.
claude-img-sem-tamanho-natural-em-flex.html

# lote imagens-no-raster — a altura medida (16) é o ícone de imagem PARTIDA
# do Chromium (sem `alt`, `src` que não resolve neste corpus): não é uma
# constante de CSS2 §10.3.2/§10.6.2 (cujo fallback seria 150) nem algo que
# este motor calcule — não há razão de aspeto disponível para derivar
# NADA (nem 16 nem 225). `layout::tests::imagens_corpus` fixa o que o motor
# faz hoje (altura 0).
claude-img-aspect-ratio-sem-loader.html
