# O plano do motor HTML/CSS/DOM

O que este crate (e os que o rodeiam: `rts-dom-bridge`, `rts-egui`) ainda tem
de fazer, na ordem em que se faz, escrito para que um agente — ou uma pessoa —
possa pegar num lote sem ler mais nada primeiro, e para que quem pare a meio
saiba onde retomar.

Vive aqui e não em `docs/` pela regra 2 de `docs/README.md`: um plano vai
stale no momento em que o trabalho começa, e quem repara é quem edita o crate.
A imagem do que o motor É (o veredito, os findings, a evidência) está em
`docs/ui/html-engine/analises/2026-09-04-auditoria-estrutural/README.md`; este
ficheiro é o que se FAZ com ela. Não repete o que lá está.

---

## 0. Como retomar — LEIA ISTO PRIMEIRO

**A tabela abaixo é o estado.** Quem fecha um lote actualiza a linha dele
**no mesmo commit** que o fecha (hash, data, o que mudou nos números). Quem
começa um lote muda o estado para "em curso" com o nome da branch. Um lote sem
linha aqui não existe.

| lote | nome | vaga | estado | branch / commit | régua que o mede |
|---|---|---|---|---|---|
| A | contrato DOM→JS | 1 | ☑ integrado em `feat/dom-vaga-1` (2026-09-04) | `feat/dom-contrato-js` → `bda7e065`+`667f2609` | `cargo test -p rts-dom-bridge`; `tests/claude-dom-getboundingclientrect.test.ts` |
| B | um medidor activo | 1 | ☑ integrado em `feat/dom-vaga-1` (2026-09-04) | `feat/dom-medidor-ativo` → `4c3ee981` | testes em `rts-dom` (medidor falso) |
| C | `position` relative/absolute | 1 | ☑ integrado; corpus 6/6 desvios fechados | `feat/dom-position-relativo-absoluto` → `a2d6dde1` | corpus: `claude-position-*` (6 desvios) |
| D | grid: rows por áreas | 1 | ☑ integrado; corpus 3/3 fechados | `feat/dom-grid-areas-rows` → `161c532a` | corpus: `claude-grid-areas` (3) |
| E | BFC, floats, `clear` | 1 | ☑ integrado; corpus 6/6 fechados, suite sem perdidos | `feat/dom-bfc-floats-clear` → `cf592ba0` | corpus: `claude-clear`, `claude-float-clear` (6) |
| F | baseline, `vertical-align`, `white-space` | 1 | ☑ integrado; corpus 8/8 fechados (o 8.º por C/E) | `feat/dom-linha-baseline` → `ebe268ef` | corpus: `claude-vertical-align`, `claude-white-space`, `claude-text-align` (8) |
| G | scroll no documento | 2 | ☑ integrado; fixture verde; janela real POR MEDIR | `feat/dom-scroll-no-documento` → `951a72b2` | teste de `scrollTop`/`scrollTo` + exemplo em janela |
| H | o escopo de página não vê o Node | 2 | ☑ integrado; fixture verde; `eval` indirecto ainda vê `process` (§4.H) | `feat/dom-escopo-pagina-sem-node` → `8e30fcc1` | fixture `claude-dom-page-nao-ve-process` |
| I | folha de UA em CSS real | 2 | ☑ `style/ua.css` parseada pelo mesmo parser, origem UA na cascade (inversão no `!important`); `ua_display` fora do layout; sensibilidade a atributo por NOME; `em`/`rem` em margens no `getComputedStyle`. PARCIAL: `UA_TABLE` ainda alimenta o eixo display/iniciais; `scrollbar.rs` só desenhado. Fixtures `claude-ua-*` medidas no Blink e a FALHAR (15 desvios = lacunas do motor: largura de texto a negrito/controlos, fonte dos controlos, `tr` com `border-spacing`) | `feat/dom-lote-i-folha-ua` → `615983cf`+`1b9e817e` (2026-09-04) | `<th>` negrito/centro; `scrollbar.rs` apagado |
| J | `DeclarationRecord` e `revert` | 2 | ☑ `revert`/`revert-layer` por marcador, resolvidos sobre as regras casadas do elemento (sem lista por propriedade — custo zero sem `revert`, provado por teste); fixtures `claude-revert*` medidas no Blink e a PASSAR; suite 859/888 sem perdidos | `feat/dom-lote-j-declaration-record` → `7bd05644` (2026-09-04) | fixture `revert`/`revert-layer` medida no Chrome |
| K | invalidação escopada para `:nth-child` | 2 | ☑ subárvore do pai em vez de `touch()` global; testes de escopo; corpus 49/49 e suite 858/887 sem perdidos; TEMPO por medir (`dom_metrics`) | `feat/dom-lote-k-invalidacao-escopada` (2026-09-04) | `dom_metrics`: cascades por `appendChild` |
| L | cache de fragmentos em flex/grid/tabela | 2 | ☑ itens de flex-row/coluna/grid reusam (`FragmentKey` + tamanhos impostos); tabela e out-of-flow NÃO; corpus 49/49, suite 858/887 sem perdidos, paridade em 6 páginas: desvio máx. 6,1e-5 px (artefacto do reuso); `fragment_hits > 0` provado por teste; número do `dom_metrics` numa app real por medir | `feat/dom-lote-l-cache-flex-grid` → `3bccf4ea` (2026-09-04) | `dom_metrics`: subárvores reusadas numa app flex |
| M | ciclo de vida do nó | 2 | ☑ geração por NÓ, freelist, `releaseSubtree` decidido pela fachada; fixture `claude-dom-node-lifecycle` verde; wrappers fracos BLOQUEADOS por #2636 (`WeakRef` retém) | `feat/dom-lote-m-ciclo-de-vida` → `fe750c8b`+`b2e54326` (2026-09-04) | teste: arena não cresce ao remover/inserir N vezes |
| N | réguas no CI | 2 | ☑ `dom-rulers` e `dom-tests` VERDES no runner (run 33840034384, 2026-09-04); `dom-rulers` corre com `if: !cancelled()` porque a matriz `build` teve o macOS vermelho de 03/09 a 04/09 (#2632, corrigido em #2633); ficam por fazer a escrita automática do número no README e a régua de pintura | `main` (#2629, #2633) | resumo do job + check vermelho |
| O | selectores que faltam | 3 | ☑ `:target` (com `setLocationHash` no bridge e `fixar-hash` no corredor), `:scope`, `:default`, `:placeholder-shown`, `:active`/`:visited` (estado no `Dom`), `::marker` (cor), `:has()` (invalidação global quando presente, pinada). Não feitos: `::first-line`/`::first-letter`, `:autofill`, `:modal`, `:focus-visible` real | `feat/dom-lote-o-selectores` → `1baa405f`+`4e4b5540`+`3df13ad5` (2026-09-04) | `claude-sel-*` (3 de 4 passam; `sel-has` é do lote S-inline) |
| P | at-rules | 3 | ☑ `@media` completo (intervalos, `not`/`only`, listas OR, `orientation`, `resolution`, `prefers-*` com valor do host), `window.matchMedia` real (`mediaMatches` no bridge), `@import` resolvido na fachada, `@property` (registo; sem validação de `syntax`), at-rules ignorados contados | `feat/dom-lote-p-at-rules` → `b9c317b6`+`66fc9183`+`5b57c1e6` (2026-09-04) | `claude-media-completo`, `claude-property`, `claude-import` (3/3 passam) |
| Q | CSSOM | 3 | ☐ | — | §5 |
| R | grid e flex completos | 3–4 | ☑ colocação por linhas, colunas implícitas, `dense`, alinhamento, piso de min-content no `flex-shrink`, `*-reverse`, `align-content` multi-linha; e (vaga 4) `repeat(auto-fill\|auto-fit)`, `minmax` com lados intrínsecos, `fit-content()`. `auto-fit` não suprime o gap da track colapsada (dito) | `feat/dom-lote-r-grid-flex` → `bad665db`+`902aa6ea`; `feat/dom-lote-grid-intrinseco` → `7e13d037f` (2026-09-04) | `claude-grid-*`, `claude-flex-*` (12/12 passam) |
| S | propriedades sem efeito — grupo TEXTO | 3 | ☑ `word-spacing` (quebra, pintura e largura intrínseca), `tab-size`, `line-clamp`, `text-wrap` alias, `url()` serializado com aspas (e `background-image`/`mask-image` passam a responder), rect de bloco em linha = border box. `list-style-image` parcial (o marcador pinta a imagem, ninguém a carrega). Três remendos ao modelo de inline REVERTIDOS (partiam `display-basico`) → lote S-inline | `feat/dom-lote-s-texto` → `41031be5`, `c6243732`, `81845e2a`, `291ae43e` (2026-09-04) | `claude-text-overflow`, `word-spacing`, `tab-size`, `line-clamp` (4/4 passam), `list-style-image` |
| S-inline | caixa inline por FRAGMENTOS de linha | 4 | ◐ o corte mínimo: um inline SEM conteúdo nunca é promovido a caixa e é 0×0 na linha (`sel-has` passa; `display-basico` continua). A pintura de fundo/padding/borda por fragmento para inlines COM conteúdo continua a ser a promoção a caixa — declarado, não feito | `feat/dom-lote-s-inline` → `7869dc066` (2026-09-04) | `claude-sel-has`, `claude-display-basico` (2/2 passam) |
| S-transform | transformações 2D | 4 | ☑ `matrix()`, `skew*`, composição na ordem da spec, `transform-origin`, bounding box transformada (e dos descendentes), o fluxo não muda; pintura de rotação/skew no egui continua APROXIMADA (anchor exacto, w/h por norma das colunas) | `feat/dom-lote-transformacoes` → `508181548` (2026-09-04) | `claude-transform-*` (3/3 passam) |
| U | composição e pintura | 3 | ☐ | — | §5 |
| U-pintura-1 | rotação e recorte na pintura | 5 | ☑ a matriz viaja na `DisplayList` (`PushTransform`/`PopTransform`); o rasterizador e o egui pintam quadriláteros transformados; o `BeginClip` do `overflow` passa a conter os filhos (nunca continha); `visible` num eixo só computa `auto`; o rasterizador lê `fixar-hash`. Régua de pintura: as 4 fixtures acima de 2 % → 0 %, 0 %, 0,38 %, 0,05 %; corpus 84/86 ≤ 0,5 %, nenhuma > 2 %. Não feitos: cantos redondos sob matriz, blur da sombra, glifos rodados no egui | `feat/dom-lote-pintura-rotacao-clip` → `68011c503` (2026-09-04) | `scripts/css_pintura.md` |
| N-pintura | a régua de PINTURA (screenshot-diff contra o Blink) | 5 | ☑ `claude-raster` (rasterizador headless da DisplayList, PNG sem crate nova), `css_fixtures_screenshot_edge.mjs` (captura no Edge por CDP), `css_pintura_comparar.mjs` (diff por pixel, texto mascarado e reportado). Verificado: cor sólida 0%, gradiente 0,24%, box-model 0,01%. Não pinta texto nem imagens (dito). PNG não versionados | `feat/dom-regua-de-pintura` → `d415d9124` (2026-09-04) | `scripts/css_pintura.md` |
| réguas-6 | as réguas da vaga 6 ANTES do código | 6 | ☑ 4 fixtures medidas no Edge 152 (instrumento validado: 1 632 números dos 86 esperados, desvio 0): `claude-font-unidades-ch-ex` (T: `10ch`=87,97px, `10ex`=78,44px, `line-height: normal`=19px, fallback de família), `claude-inline-fragmentos` (S-inline-2: span que quebra = união 102,77×63 a y=-2, contentor 60 e não 22), `claude-hyphens-manual` (`&shy;` quebra: 40 vs 20), `claude-border-juncao` (pintura: junção diagonal das bordas, 0,13 %). Pintura lida: `triangulo-de-borda` 1,58 % É a junção por trapézio; `object-fit` 1,95 % é o rasterizador a não mascarar imagens (instrumento, não motor) | `docs/dom-reguas-vaga-6` (2026-09-04) | corpus 83/90, 7 esperadas |
| S-hifen | `hyphens: manual` — o hífen suave | 6 | ☑ `layout/hifen.rs`: o U+00AD é oportunidade de quebra (a linha fica com o maior prefixo que cabe com "-"), não pesa nem se pinta quando não quebra (também na largura natural de `medida.rs`), `none` apaga-o antes de medir; `auto` = `manual` (sem dicionário, dito). Um gancho só no `wrap_runs` (fecho do aglomerado que não cabe). Corpus 84/90 (`hyphens-manual` passa), pintura 0,09 %, suite 859/888 sem perdidos, 872 testes | `feat/dom-lote-s-hifen` → `39598b9ac` (2026-09-04) | `claude-hyphens-manual` |
| U-pintura-2 | junção diagonal das bordas; imagens mascaradas na régua | 6 | ☑ `DisplayItem::Quad` (quadrilátero convexo): com lados adjacentes de cores diferentes cada lado sai como o trapézio do canto exterior ao interior (`pintura::trapezios_dos_lados`); com cores iguais ficam as barras; `transparent` não emite. Rasterizador com `fill_quad` por varrimento; egui por mesh. `claude-raster` mascara `Image`/`Pixels` como mascara texto. Pintura: `triangulo-de-borda` 1,58 % → 0,04 %, `border-juncao` 0,13 % → 0,02 %; corpus de pintura 87/90 ≤ 0,5 %. ACHADO: `object-fit` fica em 1,95 % porque o motor não emite o FUNDO de um `<img>` sem imagem carregada (o rasterizador pintou 0 itens) — próximo alvo de pintura. Suite 859/888 sem perdidos; 875 testes | `feat/dom-lote-u-pintura-2` → `b4eebfac5` (2026-09-04) | `claude-border-juncao`, `claude-triangulo-de-borda` |
| S-inline-2 | inline com superfície por FRAGMENTOS de linha | 6 | ☑ `inline_por_fragmentos` (superfície sem width/height/margem) fica no fluxo: arestas como átomos (`ArestaInicio/Fim`), caixa = união dos fragmentos com padding/borda verticais, `inline_fragmentos::Superficies` pinta fundo e barras por linha (esquerda só no 1.º, direita só no último). A pergunta corrigida nos 5 sítios (`is_block_level`, `is_inline_block`, 3× `vertical.rs`). Cortes: cores cruas, sem radius nos fragmentos. Corpus 85/90, pintura 0,56 % → 0,06 %, suite 859/888 sem perdidos, 876 testes; paridade: 3 páginas 0 movidos, 3 movem os links da nav de 34 → 40,4px (= Blink: 14×1,6+18) | `feat/dom-lote-s-inline-2` → `add946b5e` (2026-09-04) | `claude-inline-fragmentos` |
| img-fundo | fundo do `<img>` sem pixels | 6 | ☑ `layout_image` emite o `background` na caixa reservada, com ou sem pixels (cor crua: sem `filter`/`opacity`, sem borda/padding — dito). Pintura `object-fit` 1,95 % → 1,22 %; o resto é a imagem `data:` que nenhum loader entrega ao `<img>` (o mesmo de `list-style-image`). Suite 859/888 sem perdidos; 876 testes | `feat/dom-lote-img-fundo` → `53b96700f` (2026-09-04) | `claude-object-fit` (pintura) |
| T | fontes: `ex`/`ch`, `line-height: normal`, `font-family` como lista | 6 | ☑ `Dimension::Ex`/`Ch` resolvem por `X_HEIGHT_RATIO`/`MONO_ADVANCE` (calibrados; `10ex` 78,6 vs 78,44 Blink, `10ch` 87,97 exacto; ABI `-1` como `calc`); `font-family` guarda a LISTA serializada como o Blink e `is_mono_family` percorre-a (a primeira família conhecida decide; desconhecida = indisponível). `line-height: normal` já batia (19px). NÃO feito, dito: fonte real (`@font-face`, `rustybuzz`, kerning, UAX#14) — a decisão de crate de §5.T fica aberta. Corpus 86/90 (só UA + cursor), pintura 2,45 % → 0,27 %, 878 testes, suite 859/888 sem perdidos | `feat/dom-lote-t-fontes` → `944161f7f` (2026-09-04) | `claude-font-unidades-ch-ex` |
| V-img | imagens `data:` no `<img>` | 7 | ☑ ponte `setImageDataUrl` (base64 + PNG 8 bits, 5 tipos de cor, sem entrelaçado — o resto responde 0) → `Dom::set_pixel_data` (o caminho do canvas) → `DisplayItem::Pixels`; `Dom::image_dims` (pergunta única nos 5 sítios); loader no passo 3 de `loadResources` e no corredor; `getComputedStyle().width/height` = valor USADO (CSSOM); rasterizador pinta `Pixels` e mascara `<img>` sem pixels. NÃO feito, dito: `http(s)`/ficheiro local no loader, JPEG/GIF/WebP, `object-fit` além de `fill`, `list-style-image`/`background-image` por URL. Corpus 87/91 (`img-natural` passa), medições 2 152/2 168, 879 testes, bridge 6, suite sem perdidos, paridade 0 movidos | `feat/dom-lote-v-img` → `930c78df5` (2026-09-04) | `claude-img-natural`, `claude-object-fit` (pintura) |
| V-img-2 | `<img>` de ficheiro local; o `<img>` é inline e senta na baseline | 7 | ☑ ponte `setImageFile` (PNG do disco → pixels no documento); `loadResources` resolve o `src` contra a base; o `<img>` sai da classificação de BLOCO em `vertical.rs`/`is_block_level` (partia `abc <img> def` em três linhas assim que a imagem chegava) e o átomo `Replaced` senta na baseline (y=15, não 4). NÃO feito, dito: `http(s)` (sem `fetchBytes` no motor novo), JPEG/GIF, `object-fit` além de `fill`. Corpus 88/92, medições 2 168/2 184, 879 testes, bridge 7, suite sem perdidos, paridade 0 movidos; a fixture pinta a 1 % só porque o rasterizador não lê ficheiros | `feat/dom-lote-v-img-2` → `17bec5631` (2026-09-04) | `claude-img-ficheiro` |
| flex-limites | `max-width`/`min-width` e margens `auto` no item flex; comprimentos computados em px | 7 | ☑ `FlexItem::max_main` (clamp da base e do grow — sem redistribuir o excedente, dito), `min-width` declarado substitui o piso de min-content, margens `auto` repartem o espaço livre antes do `justify-content`; o item com margem `auto` recebe o seu `main` como largura disponível (senão centrava duas vezes); `flex_limites.rs` extraído. `getComputedStyle` responde comprimentos relativos em px (`42em` → `672px`). **Régua de página real** (`scripts/parity/`, Edge): Bootstrap cover 36 → 39 elementos dentro de 1px | `feat/dom-lote-flex-max-width` → `9a62ffb57` (2026-09-04) | `claude-flex-item-max-width` |
| flex-item-bfc | um item de flex/grid contém os seus floats; o item de coluna que estica mede-se à largura do contentor | 7 | ☑ `establishes_block_formatting_context` pergunta pelo display do pai (Flexbox §4, Grid §6); o flex-column media a altura de um item que estica em shrink-to-fit (dois floats um debaixo do outro: 70 onde o Blink dá 40). Bootstrap cover 39 → **45** dentro de 1px; corpus 90/94, 881 testes, suite sem perdidos | `feat/dom-lote-flex-max-width` → `9d0044014` (2026-09-04) | `claude-flex-item-contem-floats` |
| load-resources | `loadResources` vivo no motor novo | 7 | ☑ `readTextFile` na ponte (`recursos.rs`): o `dom.ts` chamava `fs.read_text`/`fetch.fetchText`, globais do motor ANTIGO — nenhuma `<link rel=stylesheet>` local carregava fora dos testes e o `view.ts` do README morria em `rts:fs`. `http(s)` responde "" (dito: sem busca síncrona no motor novo). `view.ts` por `node:fs`/`process.argv`. Suite sem perdidos | `fix/dom-load-resources-motor-novo` → `d73e5987b` (2026-09-04) | — |
| intrinseco-whitespace | a largura intrínseca colapsa o whitespace do HTML | 7 | ☑ `intrinsic_content_width` e os nós de texto soltos medem o texto COLAPSADO (CSS Text §4.1; `pre`/`pre-wrap` preservam) — o botão fixo do Bootstrap cover media 103px a mais por catorze espaços de indentação. Bootstrap cover 45 → **45** de 57 a 1px; corpus 91/95, 882 testes, suite sem perdidos | `feat/dom-lote-intrinseco-whitespace` → `3996aa6ce` (2026-09-04) | `claude-intrinseco-whitespace` |
| svg-atributo | `width`/`height` do `<svg>` como comprimentos CSS; margens no placeholder | 7 | ☑ `1em`/`50%`/`24` resolvem (SVG 2 §7, presentation attributes) — o ícone `1em` do botão de tema caía no viewBox; o placeholder respeita `margin` como o `<img>`. Bootstrap cover 45 → **45** de 57 a 1px; corpus 92/96, 883 testes, suite sem perdidos | `feat/dom-lote-svg-atributo-em` → `828db35a8` (2026-09-04) | `claude-svg-atributo-em` |
| borda-por-lado-intrinseca | a borda por lado na largura intrínseca e na base flex | 7 | ☑ `child_outer_width`/`content_natural_width`/`flex_base_outer`/`limites_do_item` somam `used_widths` (esq+dir) em vez da borda escalar — o caret `::after` do Bootstrap (só bordas) media 0. Bootstrap cover 45 → **45** de 57; corpus 93/97, 884 testes, suite sem perdidos | `feat/dom-lote-borda-por-lado-intrinseca` → `9633f3500` (2026-09-04) | `claude-borda-por-lado-intrinseca` |
| inline-block-bfc | um inline-block flui os filhos como BLOCO; a corrida de inline-blocks alinha pela baseline própria | 7 | ☑ `to_display_code`: `Inline`/`InlineBlock` → eixo 0 (era "wrap": os filhos de qualquer inline-block iam pelo colocador horizontal do flex, alinhados pelo topo); `linha_ib` com `envelope_com_baseline`/`topo_do_item_com_baseline` (baseline própria: texto = borda+padding+meia+ascent, vazio = fundo, controlo = texto), strut pela caixa de linha (`line-height`), inline-block vazio na linha senta na baseline. O caret `::after` do Bootstrap a y=9. Corpus 93/97 (só UA + cursor), suite sem perdidos, paridade 0 movidos, 884 testes | `feat/dom-lote-borda-por-lado-intrinseca` → `4fd967d09` (2026-09-04) | `claude-borda-por-lado-intrinseca` |
| N-wpt | a régua dos REFTESTS do WPT | 7 | ☑ `scripts/wpt_reftests.mjs`: teste e referência rasterizados pelo `claude-raster` e comparados pixel a pixel (auto-consistência, sem browser). `css/css-flexbox`, primeiros 300 de 489: **114 passam (38 %)**; piores: baseline multi-linha, tamanhos definidos, `writing-mode` vertical, testes com `<script>` (sem JS). A família `testharness.js` (528 testes nas mesmas pastas) fica por montar | `feat/dom-lote-borda-por-lado-intrinseca` (2026-09-04) | `scripts/wpt_reftests.md` |
| ib-nowrap | a corrida de inline-blocks respeita `white-space: nowrap` | 7 | ☑ `linha_ib.rs` só quebra com `normal`/`pre-wrap`/`pre-line` — a referência de 27 reftests de flexbox do WPT (quatro inline-blocks de 6em num `nowrap` de 12em) quebrava em duas linhas aqui | `feat/dom-lote-ib-nowrap` → `ebdf25149` (2026-09-04) | `claude-inline-block-nowrap` |
| justify-fisico | `justify-content: left`/`right` são físicos | 7 | ☑ variantes `Left`/`Right` (Box Alignment §5.1): `left` encosta à esquerda mesmo em `row-reverse`, numa coluna valem `start`; resolvidos antes do espelho (`coluna::fisico_para_eixo`); serializados como `left`/`right`. 16 reftests "justify" do WPT | `feat/dom-lote-ib-nowrap` → `7e50df0f0` (2026-09-04) | `claude-justify-left-right` |
| clearfix | `::after{display:block;clear:both}` contém os floats | 7 | ☑ `clearfix.rs`: o fim do fluxo do contentor desce até ao fundo dos floats que o pseudo de bloco com `clear` nomeia (§9.5.2) — a referência de 20 reftests "wrap" do WPT. CORTE dito: `content`/`height`/fundo do pseudo de bloco não são desenhados. Corpus 96/100, 887 testes, suite sem perdidos; WPT css-flexbox 186 → **193/489** | `feat/dom-lote-ib-nowrap` → `7e50df0f0` (2026-09-04) | `claude-clear-em-pseudo` |
| pseudo-flex-item | `::before`/`::after` de um flex são itens flex | 7 | ☑ `flex_pseudo.rs` mede (conteúdo, padding, borda, margem) e pinta (fundo, barras, texto) o pseudo; `flex.rs` põe o `::before` antes e o `::after` depois dos filhos; a largura intrínseca do flex em linha conta-os (Flexbox §4). O caret do botão do Bootstrap entra: 42 → 52,08 (Blink 54,08 — os 2px são `.3em` de borda por lado que `resolved_sides` só lê em px: próximo lote). CORTE dito: radius/flex-basis/min-max do pseudo; texto não quebra. Corpus 97/101, WPT flexbox 194/489, 888 testes, suite sem perdidos, paridade 0 movidos | `feat/dom-lote-pseudo-flex-item` → `100cfac0e` (2026-09-04) | `claude-pseudo-item-flex` |
| borda-em | bordas em `em`/`rem` resolvem contra a fonte | 7 | ☑ `set_side_width_dim`/`parse_width_dim`: os lados guardam a DIMENSÃO e `resolved_sides` resolve-a contra a fonte computada; o shorthand uniforme com `em` escreve os quatro lados (a fonte do nó não está computada no parse) e deixa uma estimativa no escalar — pelos lados a pintura perde o `border-radius`, dito. O caret do Bootstrap fica com 9,6: botão 55,68 (Blink 54,08; era 52,08 — passou de −2 a +1,6, a régua da cover não fecha só com isto). WPT flexbox 194 → 193: `auto-margins-001` passava VAZIO (o `border: 1em` do teste e da referência caía a 0 e nada se pintava); com bordas reais aparece o defeito verdadeiro — `margin: auto` no eixo transversal do flex não centra — que é lote próprio. Corpus 98/102, 889 testes, suite 859/888 sem perdidos, paridade 0 movidos | `feat/dom-lote-borda-em` → `1aca4fdc2` (2026-09-04) | `claude-borda-em` |
| inline-flex | `display:inline-flex` é flex por DENTRO e inline-level por FORA | 7 | ☑ `DisplayKind::InlineFlex` — `to_display_code` devolve o MESMO código de `Flex` (2, horizontal) para o eixo dos FILHOS, e entra nos seis sítios que já tratavam `InlineBlock` como inline-level/atómico (`is_inline_level`, `is_inline_block` em `caixa.rs`, a 5.ª cópia em `vertical.rs`, os dois `matches!` de `establishes_block_formatting_context` e o de `margin_child_role`, todos em `bloco.rs`) — sem isso um `inline-flex` sem `width` nunca encolhia (tomava a largura do bloco) e nunca ficava na linha do pai (um bloco por linha). `getComputedStyle().display` responde `inline-flex` (`style/fmt_values.rs`, braço exaustivo — o compilador forçou). RETRABALHO 1 ronda: o corte original ("`flex-wrap:wrap` não quebra") derrubava `gap-006-ltr/-rtl/-lr/-rl` do WPT flexbox, que passavam ANTES do lote (como bloco flex com wrap) — `DisplayKind::InlineFlexWrap` nova (espelha `Flex`/`FlexWrap`; sintetizar `FlexWrap` para um `inline-flex` perderia o outer-display, o MESMO bug escondido atrás do wrap), nos mesmos seis sítios. Confirmado com `claude-paint-dump`: `gap-006-ltr` e a sua referência dão rects IDÊNTICOS elemento a elemento. 908 testes. Corpus 112/116, **WPT flexbox 275 → 288/489** (+13: os 8 flexbox-baseline-*, percentage-heights-004, percentage-size-subitems-001, flex-inline e os gap-006 recuperados no retrabalho; zero perdidos, zero encravados), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-inline-flex` → `6f513b809` (2026-09-04) | `claude-flex-inline-flex-inline-level`, `claude-inline-flex-outer-display`, `claude-inline-flex-wrap` |
| cores-nomeadas | os 148 nomes de cor do CSS Color 4 | 7 | ☑ `style/cores_nomeadas.rs`: tabela da spec ordenada (pesquisa binária, `grey`→`gray`); `named_color` tinha 24 nomes à mão e `lightblue`/`tomato`/`rebeccapurple` caíam como declaração inválida — a caixa ficava sem fundo. Foi a causa que a triagem do WPT flexbox (9 agentes Sonnet) apontou em TRÊS baldes: **WPT 193 → 253/489** (+72 ganhos; −12 que passavam VAZIOS com `lightblue` invisível dos dois lados e agora expõem o `align-items: baseline` em falta — lote próprio, já mapeado). Corpus 99/103, 891 testes, suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-cores-nomeadas` → `6e115a388` (2026-09-04) | `claude-cores-nomeadas` |
| flex-margens-auto | margens `auto` no eixo transversal do flex | 7 | ☑ `flex_margens_auto.rs`: as margens `auto` absorvem o espaço livre transversal e vencem o `align-self` (§8.1); o item não estica. Só a LINHA: em coluna o layout de bloco do item já resolve `margin-left/right: auto` — fazê-lo em `coluna.rs` centrava duas vezes. Corte dito: item de coluna com `width: auto` + margens `auto` fica esticado em vez de fit-content. Fecha o `auto-margins-001` que o borda-em destapou. Corpus 100/104, WPT flexbox 254/489, 894 testes, suite 859/888 sem perdidos, cover 45/57 | `feat/dom-lote-flex-margens-auto` → `100906a61` (2026-09-04) | `claude-flex-margens-auto-transversal` |
| pintura-e-caixas | z-index negativo, `::before`/`::after` de bloco, flex evita float | 7 | ☑ `empilhamento.rs` prepende os itens de `z-index` negativo à `DisplayList` em vez de os pintar depois do fluxo (CSS 2.1 Apêndice E, passo 3), corrigindo `at`/`hit_at`/`filhos_antes`/`filhos_dentro` de quem já lá estava; `pseudo_bloco.rs` mede e pinta um `::before`/`::after` de bloco com conteúdo não-vazio como o primeiro/último filho do fluxo vertical (`runs.rs::pseudo_run` parou de os tratar como inline, para não duplicar o texto); `bfc_evita_float.rs` desce um contentor que ESTABELECE BFC (`display:flex` incluído) para debaixo de um float anterior que não cabe ao lado, em vez de o sobrepor. Um teste antigo (`pseudo::tests::display_block_no_pseudo_...`) fixava o comportamento ERRADO ("tratado como inline") — corrigido e reforçado com uma prova por altura, não só por texto. CORTES ditos: só largura fixa/resolvível decide o desvio do float (não a que devia ENCOLHER); pseudo de bloco só quando o DONO é um contentor de fluxo vertical. `cargo check -p rts-dom` limpo; `cargo test -p rts-dom --lib --features metrics` 897, 0 falhas (894 → 897, as 3 do corpus novo). 905 testes. Corpus 109/113, **WPT flexbox 271 → 275/489** (+4: flexbox_nested-flex, flexbox_generated-nested-flex, flexbox_fbfc, flex-box-wrap; zero perdidos, zero encravados), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-pintura-e-caixas` → `0b351290a` (2026-09-04) | corpus: `claude-z-index-negativo-atras-do-fluxo`, `claude-pseudo-after-display-block`, `claude-flex-bfc-evita-float` |
| flex-basis-piso | piso de min-content sem grow; `flex-basis` unitless inválida; espaço antes do `:` | 7 | ☑ três causas pequenas e independentes da triagem WPT flexbox. (1) `com_piso_minimo` (`flex_limites.rs`): o piso automático de min-content (§4.5) só era aplicado no laço de encolhimento — um item `flex-grow:0` nunca entrava lá e ficava preso em `flex-basis:0`; agora o piso vale para TODO item fora desse laço, com `grid_cols` isento (coluna de grid é largura fixa por desenho). (2) `apply_flex_shorthand` (movida de `mod.rs`, no teto, para `fluxo.rs`): um 3º token unitless não-zero (`flex: 0 0 4`) invalidava só o token, não o shorthand inteiro (CSS Values §6.1). (3) `declaracao_nome.rs` (novo módulo, extraído de `syntax.rs`, no teto): um espaço antes do `:` descartava a declaração inteira (CSS Syntax §5.4.4 permite espaço dos dois lados). `flex.rs` ficou nas mesmas 499 linhas; `style/parse/mod.rs` encolheu (813→768) e `syntax.rs` também (1122→1105) — lógica nova só em módulo novo. Teste antigo `flowtests::o_grow_reparte_a_sobra_na_proporcao_dos_pesos` corrigido (fixava o `a<0` errado). RETRABALHO 1 ronda: o piso subia um item ACIMA da largura especificada e `flex-aspect-ratio-resize-001` passou a ENCRAVAR o raster (um canvas gigante) — `min_automatico` = min(min-content, largura especificada), Flexbox §4.5; um teste pina-o. 902 testes. Corpus 106/110 (o runner passou a tolerar 0,01px em comprimentos `px` computados: o Blink imprime `52.7812px` onde o medidor de texto em f32 dá `52.7808px`), **WPT flexbox 257 → 271/489** (+14: os 9 flexbox_flex-0-* do piso, os 4 unitless-basis e flex-direction; zero perdidos, zero encravados), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-flex-basis-piso` → `7d4696c39` (2026-09-04) | `claude-flex-basis-zero-min-content`, `claude-flex-basis-unitless-invalido`, `claude-declaracao-espaco-antes-dois-pontos` |
| pequenos-1 | três causas pequenas da triagem WPT: texto solto em `display:table`, largura intrínseca do flex em coluna, `<input>` estica no flex | 7 | ☑ (1) `table/grid.rs`: um filho de TEXTO da tabela entra numa célula anónima (o `continue` para não-elementos descartava-o com o whitespace) e `layout/texto_solto.rs` pinta texto solto ao nível de bloco com a fonte do PAI (`bloco.rs`, a 1168 linhas, pintava com a fonte por omissão: 18 e não 20); e só o ELEMENTO `<table>` recebe os 2px de `border-spacing` da UA — um `div{display:table}` media 24 e não 20. (2) `medida.rs`: um flex em coluna é o MAIOR filho, não a soma (a multi-coluna do wrap continua corte dito). (3) `input.rs`: canal `forced_outer_h` — um `<input>` num flex de 80px estica até aos 80 em vez da altura da linha. O script do WPT grava os nomes dos reftests que não rasterizam (um encravamento contava como ganho). Corpus 103/107, **WPT flexbox 257/489** (+3: table-item-flex-percentage-min-width, table-item-flex-percentage-width, position-fixed-001; zero perdidos, zero encravados), 897 testes, suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-pequenos-1` → `69cf4845b` (2026-09-04) | `claude-table-texto-solto-sem-celula`, `claude-flex-column-shrink-to-fit`, `claude-flex-stretch-input-height` |
| flex-coluna-shrink | `flex-basis`/`flex-shrink` no eixo de coluna; `row-gap` contra a ALTURA; `min-height` como altura definida | 7 | ☑ `layout/coluna_shrink.rs` (módulo novo): espelha o encolhimento iterativo de `flex.rs` no eixo vertical (`base_outer` lê `flex-basis`, `min_main_auto` é 0 sob overflow não-visível ou a altura natural sem `height` declarado); nova flag `forced_outer_h_hard` em `layout_block`/`layout_block_reusing` (só `layout_children_column` passa `true` — os outros 8 sítios inalterados, `false`) porque o `forced_outer_h` existente só cresce (stretch do flex-row/grid) e o main size de coluna precisa de poder encolher. `row-gap` percentual passa a `resolve_height` (eixo de bloco, não a largura) em `flex.rs` e `coluna.rs`. ACHADO ao medir (fora da triagem original): `align-content:normal` tinha de se comportar como `stretch` no eixo cruzado do flex-wrap (CSS Box Alignment §8.3) para `claude-gap-row-percentual-eixo` bater — a leitura ingénua dava y=60, o Edge dá 110. `avail_children` (bloco.rs) ganhou `.or(mnh_pre)` — uma linha. CORTES: o piso automático de min-height sem `height` declarado usa a altura natural pré-shrink, não uma medição de min-content real; sem `max-height`/`flex-wrap` em coluna. `cargo test --lib --features metrics`: 894 (889+5), 0 falhas; filtros `coluna`/`flex` verdes. **RETRABALHO 1 ronda** (o build central mediu a branch, corpus 111/115, cover 45/57, WPT flexbox 271→280 +11, mas 2 reftests que passavam caíram): `flex-basis:content` (Flexbox §7.2) não era reconhecida — `apply_flex_shorthand` lia o 3.º token como basis AUSENTE e caía no fallback `Percent(0%)`, colapsando o item a 0 mesmo com `flex-shrink:0`; `parse_flex_basis` (novo, `fluxo.rs`) mapeia `content`→`MaxContent`, que `base_outer` já tratava certo. `min-height:min-content` DECLARADO não era parseado e caía no automático, que zera sob `overflow` não-visível — mas o piso declarado não pode desaparecer (só `min-height:auto` zera, §4.5); `Dimension::MinContent` + `parse_dimension_min_max` (`lengths.rs`, MESMO nome/variante que `feat/dom-lote-flex-min-content` usou para `min-width`/`max-width`, para os dois lotes colidirem sem duplicar — juntas no merge do lote flex-min-content) + `coluna_shrink::min_main` (novo, centraliza declarado/min-content/automático). Verificado com `claude-paint-dump` sobre os 2 reftests do WPT E as suas referências: rects idênticos nos dois. 914 testes. Corpus 118/122, **WPT flexbox 288 → 299/489** (+11: percentage-heights-002, flexbox-definite-sizes-001/002/005, flex-flow-007/010, fieldset-as-item-overflow, flexbox-single-line-clamp-3, flexbox-break-request-vert-002a/b, flexbox-overflow-vert-005; zero perdidos, zero encravados), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-flex-coluna-shrink` → `6cf6c1e3e` (2026-09-04) | `claude-flex-column-shrink`, `claude-flex-coluna-shrink-overflow`, `claude-flex-basis-percent-shrink-column`, `claude-gap-row-percentual-eixo`, `claude-flex-definite-min-height`, `claude-flex-coluna-shrink-zero-e-min-content` |
| flex-min-content | `min-content`/`max-content` em `min-width`/`max-width` do item flex; piso automático conta filho bloco sem texto; piso de min-content no shrink-to-fit geral; base flex sem pré-capagem por `max-width`, encolhimento congela quem a viola | 7 | ☑ 4 causas independentes da triagem WPT flexbox, réguas medidas no Edge antes do código. `Dimension::MinContent` (reconhecida via `parse_dimension_min_max` — `width`/`height`/`flex-basis` continuam a descartá-la no `parse_dimension` geral) resolve, no eixo INLINE (`min-width`/`max-width`), via `crate::table::min_content` em `flex_limites::limites_do_item` (min vence max, CSS2 §10.4) — o mesmo nome/variante que o lote `flex-coluna-shrink` acrescentou para `min-height`/`max-height` no eixo de bloco (`coluna_shrink::min_main`); as duas versões de `parse_dimension_min_max` juntaram-se numa só função no merge deste lote, sem duplicar. `table::widths::min_content`: só o nó de TOPO da recursão respeita `floor_width`; todo DESCENDENTE usa sempre `true` — um filho-bloco sem texto com `width` fixa deixava de contar (Flexbox §4.5). `bloco.rs`: shrink-to-fit ganha o piso que faltava na fórmula de CSS2 §10.3.5 (`flex_limites::largura_shrink_to_fit`, extraída para não crescer o ficheiro, já acima do tecto). `flex.rs`: a base do item deixa de ser pré-capada por min/max no construtor (Flexbox §9.2 passo 3) — o clamp final entra uma vez só, para TODO item, em `flex_limites::com_limites_finais` (ex-`com_piso_minimo`); o laço de encolhimento (extraído para `flex_limites::resolve_grow_encolhe`, para caber no teto) congela também por violação de `max_main`, com a fórmula do défice corrigida para conservar o total da linha quando um item congela ACIMA da base (a antiga só conservava para baixo). CORTE dito: a 2ª metade da causa min-intrinsic (célula de tabela com flex lá dentro, `cell_min_max` ramo `fixo`) fica por fazer — a triagem não a confirmou por probe, sem fixture; `flexbox_stf-table-singleline`/`-2` não devem fechar aqui. `cargo test -p rts-dom --lib --features metrics` 903, 0 falhas (896 sem a feature), sem perdidos nos testes de shrink existentes; medido DEPOIS do merge com `pintura-e-caixas`/`pequenos-1`/`flex-coluna-shrink`, e depois de novo (918) com `flex-cross-size`. Corpus 133/137 (os 4 de sempre), cover 45/57, **WPT flexbox 312 → 315/489** (+4: flex-item-min-width-min-content-max-width, min-size-auto-overflow-clip, flex-base-size-ignores-max-width, negative-margins-001). **RETRABALHO 1 ronda** (o build central mediu a branch já com o main — `48323d43d` —, e `flexbox-flex-wrap-flexing` que passava caiu): a QUEBRA de linha (`flex-wrap`, Flexbox §9.3) lia `it.base` diretamente para decidir se um item cabe na linha — e a spec soma o HYPOTHETICAL main size (a base já grampeada por min/max), não a base nua; um item `flex:1` (`flex-basis:0%`) com `min-width:100px` tinha base=0 (a pré-capagem que este lote tirou do construtor era exatamente o que escondia isto), e dois desses itens cabiam sempre juntos em vez de quebrar pelo seu piso declarado. Fix: `hyp = flex_limites::com_limites_finais(it.base, it.min_main, it.max_main, grid_cols)` computado só para a soma da quebra — `it.base` continua intacta no item, para o orçamento de grow/shrink de cada linha (§9.2 passo 3), que ainda parte da base pura. Confirmado com `claude-paint-dump` sobre o reftest e a referência do WPT: os dois itens verdes cobrem exatamente o contentor vermelho de 150×100 (150×50 cada, empilhados), igual à referência. `flex.rs` 439 linhas (era 432). `cargo test --lib --features metrics`: 923 antes do retrabalho → **924** (+1 teste), 0 falhas. 933 testes. Corpus 134/138, **WPT flexbox 312 → 316/489** (+4: flex-item-min-width-min-content-max-width, min-size-auto-overflow-clip, flex-base-size-ignores-max-width, negative-margins-001; zero perdidos, zero encravados; retrabalho de 1 ronda (a quebra de linha usa o tamanho hipotético grampeado)), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-flex-min-content` → `b89c2ea9f` (2026-09-04) | `claude-flex-min-width-min-content`, `claude-flex-min-width-auto-block-child`, `claude-shrink-to-fit-sem-piso-min-content`, `claude-flex-base-size-max-width`, `claude-flex-wrap-quebra-com-min-width` |
| flex-coluna-shrink | `flex-basis`/`flex-shrink` no eixo de coluna; `row-gap` contra a ALTURA; `min-height` como altura definida | 7 | ☑ `layout/coluna_shrink.rs` (módulo novo): espelha o encolhimento iterativo de `flex.rs` no eixo vertical (`base_outer` lê `flex-basis`, `min_main_auto` é 0 sob overflow não-visível ou a altura natural sem `height` declarado); nova flag `forced_outer_h_hard` em `layout_block`/`layout_block_reusing` (só `layout_children_column` passa `true` — os outros 8 sítios inalterados, `false`) porque o `forced_outer_h` existente só cresce (stretch do flex-row/grid) e o main size de coluna precisa de poder encolher. `row-gap` percentual passa a `resolve_height` (eixo de bloco, não a largura) em `flex.rs` e `coluna.rs`. ACHADO ao medir (fora da triagem original): `align-content:normal` tinha de se comportar como `stretch` no eixo cruzado do flex-wrap (CSS Box Alignment §8.3) para `claude-gap-row-percentual-eixo` bater — a leitura ingénua dava y=60, o Edge dá 110. `avail_children` (bloco.rs) ganhou `.or(mnh_pre)` — uma linha. CORTES: o piso automático de min-height sem `height` declarado usa a altura natural pré-shrink, não uma medição de min-content real; sem `max-height`/`flex-wrap` em coluna. `cargo test --lib --features metrics`: 894 (889+5), 0 falhas; filtros `coluna`/`flex` verdes. **RETRABALHO 1 ronda** (o build central mediu a branch, corpus 111/115, cover 45/57, WPT flexbox 271→280 +11, mas 2 reftests que passavam caíram): `flex-basis:content` (Flexbox §7.2) não era reconhecida — `apply_flex_shorthand` lia o 3.º token como basis AUSENTE e caía no fallback `Percent(0%)`, colapsando o item a 0 mesmo com `flex-shrink:0`; `parse_flex_basis` (novo, `fluxo.rs`) mapeia `content`→`MaxContent`, que `base_outer` já tratava certo. `min-height:min-content` DECLARADO não era parseado e caía no automático, que zera sob `overflow` não-visível — mas o piso declarado não pode desaparecer (só `min-height:auto` zera, §4.5); `Dimension::MinContent` + `parse_dimension_min_max` (`lengths.rs`, MESMO nome/variante que `feat/dom-lote-flex-min-content` usou para `min-width`/`max-width`, para os dois lotes colidirem sem duplicar) + `coluna_shrink::min_main` (novo, centraliza declarado/min-content/automático). Verificado com `claude-paint-dump` sobre os 2 reftests do WPT E as suas referências: rects idênticos nos dois. 914 testes. Corpus 118/122, **WPT flexbox 288 → 299/489** (+11: percentage-heights-002, flexbox-definite-sizes-001/002/005, flex-flow-007/010, fieldset-as-item-overflow, flexbox-single-line-clamp-3, flexbox-break-request-vert-002a/b, flexbox-overflow-vert-005; zero perdidos, zero encravados), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-flex-coluna-shrink` → `6cf6c1e3e` (2026-09-04) | `claude-flex-column-shrink`, `claude-flex-coluna-shrink-overflow`, `claude-flex-basis-percent-shrink-column`, `claude-gap-row-percentual-eixo`, `claude-flex-definite-min-height`, `claude-flex-coluna-shrink-zero-e-min-content` |
| flex-baseline | `align-items`/`align-self: baseline` no flex; `flex-wrap` de 3 estados | 7 | ☑ `AlignItems::Baseline` resolvida POR LINHA (`layout/flex_baseline.rs`, módulo novo — `flex.rs` já estava no tecto de 500): ascent do item = `margin-top` + `linha_ib::ascent_do_item` (agora `pub(in crate::layout)`, reusada em vez de duplicada); o de maior ascent fica no início da linha, os outros descem `max_ascent - ascent_i`. `FlexWrap` (NoWrap/Wrap/WrapReverse) substitui o `Option<bool>` que fazia `"wrap-reverse"` cair em `Some(false)`, idêntico a `nowrap`; `WrapReverse` agrupa como `Wrap` e só inverte a ORDEM das linhas no eixo cruzado (§8.3). **RETRABALHO** (build central: WPT flexbox 312→311, os testes que o lote devia fechar continuavam a falhar): `claude-paint-dump` teste+referência (envolvidos em `<html>` quando faltava) achou dois bugs reais. (1) o cross-size de uma linha com grupo baseline não é o maior item — é o ENVELOPE (`max_ascent+max_descent`, Flexbox §9.4 passo 8); `flex_baseline::calcula_linha` (substitui `offsets_da_linha`) devolve cross-size e offsets da mesma passada, usado no cálculo por linha E na estimativa de `align-content` — fecha `multiline-reverse-wrap-baseline` e `flexbox-baseline-multi-line-horiz-003/004` (rects idênticos à referência, confirmado por dump). (2) `AlignItems::LastBaseline` (variante própria): `align-items: last baseline` caía como declaração inválida; o corte dito é o fallback pela MARGEM INFERIOR (`align_offset` trata como `FlexEnd` — a spec pede a ÚLTIMA baseline do item, que este motor não mede) — fecha `flex-order-last-baseline{,-multiple}`. NÃO fechados, de OUTRO lote (dito, não tocado): `flexbox_align-items/self-baseline` (o grupo baseline já bate exato; falta 1px de `position:absolute` — `posicionado.rs` usa o border-box do ancestral em vez do padding-box); `css-flexbox-row-wrap-reverse` (`writing-mode` vertical, já fora de âmbito); a referência de `multiline-reverse-wrap-baseline` usa uma `<table>` com `vertical-align:bottom` que parece ter bug próprio (tabela, não flex). CORTES ditos: baseline só na LINHA (coluna cai em `FlexStart`); pseudo-item e texto solto fora do grupo; `align-content` não reespelha físicos sobre a ordem já invertida; `flex-direction:column`+`wrap` continua sem multi-coluna (pré-existente). **2.º MERGE** (`flex-column-wrap`, #2678): o campo `flex_wrap_reverse: bool` que aquele lote tinha acrescentado AO LADO de `flex_wrap` foi apagado — uma representação só, `FlexWrap::{NoWrap,Wrap,WrapReverse}`; `layout/coluna_wrap.rs` (a ordem das colunas em `wrap-reverse`) passou a ler `FlexWrap::WrapReverse`. `cargo test --lib --features metrics`: 926 (904→926, 0 falhas — 4 antigos inalterados, 2 novos fixam o envelope e o `last baseline`); `flex_column_wrap_corpus` confirmado verde após a adoção do enum. 943 testes. Corpus 138/142, **WPT flexbox 335 → 336/489** (+1: flexbox-flex-flow-002; zero perdidos, zero encravados; retrabalho de 1 ronda (envelope ascent+descent, last baseline; enum FlexWrap adotado no merge)), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-flex-baseline` → `b55b9c14c` (2026-09-04) | `claude-flex-align-baseline`, `claude-flex-wrap-reverse-linha`, `claude-flex-wrap-reverse` |
| flex-column-wrap | `flex-direction:column` + `flex-wrap` quebra em MÚLTIPLAS COLUNAS | 7 | ☑ `layout/coluna_wrap.rs` (módulo novo): agrupa os itens em colunas pela BASE do eixo principal (empacotamento guloso, o espelho de `flex.rs` no eixo trocado), `flex-grow`/`flex-shrink` por coluna (reusa `coluna_shrink::shrink`, e `min_main` do retrabalho de `flex-coluna-shrink` — o merge com o main trocou o `min_main_auto` direto por essa função nova, que também decide `min-content`), largura da coluna = o maior item dela, `align-content:normal` estica as colunas no eixo cruzado (mesma regra do lote `flex-coluna-shrink`) — media 640 (1280/2) na fixture, não 100 encostado. `wrap-reverse` troca a ORDEM DAS COLUNAS no eixo cruzado, nunca a ordem dos itens (adotado o `FlexWrap` do lote `flex-baseline` no merge com ele — ver a linha de lá; era um campo `flex_wrap_reverse` próprio até esse merge); `column-reverse` só espelha a POSIÇÃO dentro de cada coluna — o AGRUPAMENTO usa sempre a ordem do documento (confirmado item a item contra as 4 referências WPT `flexbox_flow-column{,-reverse}-wrap{,-reverse}`, que reordenam via floats). CORTES documentados no ficheiro: a largura shrink-to-fit do CONTENTOR não soma as colunas (`multiline-shrink-to-fit`, casos 2/4, fica por fazer — a fixture desse balde já dizia "A MEDIR", não confirmada no Blink); margem `auto` no eixo principal dentro de uma coluna; `::before`/`::after` como item de coluna (gap pré-existente, não deste lote). Corpus Rust `flex_column_wrap_corpus.rs`: 1 caso MEDIDO no Blink (`claude-flex-column-wrap-multicoluna`) + 1 caso `column-reverse wrap-reverse` DERIVADO da leitura do algoritmo (não medido; ordem dos itens conferida contra a referência real do WPT). MERGE com `flex-justify-logico` (3.º merge): `justify_e_align` (chamado tanto daqui como de `coluna_wrap.rs`) passou a usar `fisico_para_coluna` em vez do `Left|Right→FlexStart` antigo, e o PASSO 5 de `coluna_wrap.rs` ganhou o mesmo `coluna_rtl::cross_x` do caminho de uma coluna — espelhado DENTRO de cada coluna (`[x, x+cw]` como content-box), não do contentor inteiro; a ORDEM DAS COLUNAS não inverte com RTL (só com `wrap-reverse`), corte dito, mesma pergunta adjacente a bidi que a triagem original já deixava fora para o wrap no eixo de linha. **RETRABALHO** (o build central mediu a branch já com os 3 merges: corpus 130/134, cover 45/57, WPT flexbox 312→329 +19, mas `flexbox-flex-basis-content-004a`/`-004b` que passavam no main caíram): não era o algoritmo de `coluna_wrap.rs` — era o DESPACHO em `coluna.rs`/`bloco.rs` a confundir dois sentidos de "altura definida". `.item{flex:0 0 content;min-height:0}` sendo ele próprio `display:flex;flex-direction:column;flex-wrap:wrap` (sem `height`) tinha `avail_children==Some(0.0)` só porque `min-height:0` conta como altura definida para stretch/`height:%` (regra do lote `flex-coluna-shrink`) — mas um PISO de 0 não é um LIMIAR de wrap, e um limiar de 0 abre uma coluna nova a cada item (3 itens, 3 colunas de 1 lado a lado, em vez de uma pilha vertical de altura automática). `bloco.rs::wrap_definite_h` (novo: `explicit_content_h.or(mxh_pre)`, SEM `mnh_pre`) passa a ser o valor que decide o despacho do wrap; `avail_children` continua inalterado para tudo o resto (gap%, `height:%` dos netos, grow/shrink). Confirmado com `claude-paint-dump` sobre os dois reftests E a referência: os 6 casos (inline-blocks, floats, canvas×3, replaced, flex simples, flex multi-linha) saem pixel a pixel iguais aos dois lados nos dois reftests, incluindo o caso 6 que caiu (`.item` 55×40 com os 3 `innerItem` empilhados a x=10 constante, y=260/272/284 — antes do fix saía 55×16 com os 3 lado a lado). Teste novo, `min_height_zero_no_item_nao_e_limiar_de_wrap_para_o_seu_proprio_column_wrap`, pina o caso isolado. `cargo test -p rts-dom --lib --features metrics`: 931, 0 falhas (+1 teste). 936 testes. Corpus 135/139, **WPT flexbox 316 → 335/489** (+19: os 4 flexbox_flow-column-*wrap*, flex-flow-008/009/011/012, gap-007-*, flexbox_order, flexbox_rtl-flow(-reverse), flexbox_wrap-reverse, flexbox-flex-flow-001, flexbox-overflow-vert-004, flexbox-align-self-stretch-vert-002; zero perdidos, zero encravados; retrabalho de 1 ronda (min-height: 0 não é limiar de wrap)), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-flex-column-wrap` → `f660b14e0` (2026-09-04) | `claude-flex-column-wrap-multicoluna`; WPT `flexbox_flow-column{,-reverse}-wrap{,-reverse}` (4), `flex-flow-008/009/011/012`, `flexbox_rtl-order`, `flexbox_rtl-flow(-reverse)`, `gap-007-ltr`, `flexbox_order`, `multiline-shrink-to-fit` |
| flex-justify-logico | `justify-content:start`/`end` lógicos; `left`/`right` numa coluna físicos ao TOPO; `direction:rtl` no eixo cruzado da flex-column | 7 | ☑ `JustifyContent::Start`/`End` (Box Alignment §8.1, invariantes a `row-reverse`, resolvidos no mesmo caminho físico que `Left`/`Right` já tinham em `coluna::fisico_para_eixo`); `coluna::fisico_para_coluna` (novo: `left`/`right`/`start` colapsam em "topo" numa coluna — sem eixo próprio, Box Alignment §5.1 —, `end` fica assimétrico e vai para "fundo"; nenhum espelhado por `column-reverse`, causa 2 da triagem); `layout/coluna_rtl.rs` (módulo novo, `coluna.rs` já perto do teto: `cross_x` espelha o eixo cruzado da coluna dentro do content-box quando `direction:rtl`, identidade em `ltr`; um item de `align-items:stretch` NOMINAL com largura declarada não estica de verdade — mesmo corte de `grid.rs:355`, `ccss.width.is_none()`). WPT: `flexbox_justifycontent-start`/`-end`(-rtl), `-left-002`/`-right-002`, `flexbox_rtl-direction`. **RETRABALHO** (o build central mediu a branch: corpus 121/125, cover 45/57, WPT flexbox 299→305 +7, mas `overflow-top-left` que passava caiu): (1) `cross_x` espelhava mesmo sob `writing-mode:vertical-rl` — o motor não faz layout vertical (tudo tratado como horizontal) e a referência do WPT TAMBÉM trata esse wrapper como bloco/horizontal, então espelhar divergia mais; `style/text.rs::WritingMode` (propriedade nova, `[inh]`, parse+serialização) e `cross_x` só espelha com `writing_mode.is_horizontal()`. (2) um filho mais largo do que o contentor com `direction:rtl` transborda pela ESQUERDA no Chrome e nem o bloco nem a coluna faziam isto: `layout/rtl_bloco.rs` (módulo novo) resolve o CSS 2.1 §10.3.3 (margem esquerda = espaço livre com sinal, negativo incluído) pela `direction` do PAI de `id` — não a do próprio nó — e devolve cedo se o pai é flex/grid (esses já são posicionados pelo seu container; sem o guard, dobrava o deslocamento: 200+200=400 onde o Chrome dá 200); `coluna_rtl::cross_x` passou a receber a largura VERDADEIRA do item no espelho (não a grampeada a `content_w`, que escondia o transbordo). Confirmado com `claude-paint-dump` sobre `overflow-top-left.html`/referência: o texto do `.column-wrapper` (horizontal) sai em (-194,52) nos dois, pixel a pixel. **2.º RETRABALHO** (o build central mediu de novo: corpus 122/126, WPT +7, `overflow-top-left` CONTINUAVA perdido — agora pela metade de BLOCO): `rtl_bloco::margin_left_usado` ficou sem a mesma guarda de `writing-mode` que `cross_x` ganhou — o `.row-wrapper` do reftest (BLOCO, `writing-mode:vertical-rl;direction:rtl`) passou a encostar os filhos à DIREITA enquanto o `.column-wrapper` (flex, já guardado) os deixava à ESQUERDA, e os dois lados do teste (que antes divergiam do Chrome da MESMA forma) passaram a divergir um do outro. Mesma guarda em `rtl_bloco.rs`: só resolve a equação quando `parent_css.writing_mode.is_horizontal()`. Confirmado de novo com `claude-paint-dump`: os 4 itens de texto dos DOIS wrappers saem nas MESMAS coordenadas nos dois lados — (14,14), (-194,52), (14,98), (14,124). `cargo test -p rts-dom --lib --features metrics`: 919, 0 falhas (mesmo número — só a guarda que faltava, sem teste novo). 928 testes. Corpus 129/133, **WPT flexbox 305 → 312/489** (+7: flexbox_justifycontent-start/end(-rtl), left-002, right-002, flexbox_rtl-direction; zero perdidos, zero encravados; 2 rondas de retrabalho (writing-mode vertical fora do espelho rtl; filho a transbordar pela esquerda em rtl)), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-flex-justify-logico` → `3d8429d08` (2026-09-04) | corpus: `claude-justify-start-end-row-reverse`, `claude-justify-left-column-reverse`, `claude-flex-column-rtl-cross-start`, `claude-rtl-filho-transborda` (`layout/tests/flex_justify_logico_corpus.rs`, 5/5) |
| visibility-collapse | `visibility:collapse` distinto de `hidden` no `computedStyle` | 7 | ☑ `Visibility::Collapse` (parse + `computedProperty("visibility")` → `"collapse"`), `suppresses_paint()` a substituir o `== Some(Hidden)` duplicado em `bloco.rs`/`pintura.rs`. ACHADO ao medir (a mesma causa vista por quatro agentes na triagem — baldes `align` causas[9], `outros-2` causas[3], `gap-overflow` causas[1], `outros-3` causas[5]): as três fixtures foram medidas no Edge 152 E reconfirmadas num Chrome 152 real via `chrome-devtools` na mesma sessão, e nos três casos o Chrome NÃO implementa CSS Flexbox §algo-visibility — o item colapsado continua a ocupar a largura cheia no eixo principal, conta para o wrap e mantém os dois `gap`, inclusive no reftest OFICIAL do WPT (`gap-collapse.html`), medido do mesmo jeito e que também não fecha nesse Chrome. CORTE, por isso: a exclusão do eixo principal/wrap/gap + o "strut" de cross-size NÃO implementados — divergiriam da régua medida sem nenhuma fixture a pedi-lo; `flex.rs`/`coluna.rs` intocados. WPT que ficam de fora: `flexbox_visibility-collapse[-line-wrapping]`, `flexbox-collapsed-item-horiz-001/002` (os 7 sub-casos)`/003`, `flexbox-collapsed-item-baseline-001`, `gap-collapse`. Corpus/paridade/suite `*.test.ts`: medidos no fecho. `cargo test --lib --features metrics` medido DEPOIS de juntar `origin/main` (pequenos-1, flex-basis-piso, pintura-e-caixas, inline-flex, flex-coluna-shrink RETRABALHO)923 testes. Corpus 125/129, **WPT flexbox 305 → 305/489** (sem ganhos nem perdas: o Blink não implementa o colapso de itens flex (medido no Edge e num Chrome real), os 7 reftests ficam fora de âmbito com prova; zero encravados), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-visibility-collapse` → `703bdb529` (2026-09-04) | `claude-flex-visibility-collapse`, `claude-visibility-collapse-flex-gap`, `claude-visibility-collapse-flex-wrap` |
| flex-cross-size | o cross-size de uma linha flex vence mesmo em overflow; `align-content` transborda quando as linhas não cabem | 7 | ☑ `layout/flex_linhas.rs` (módulo novo): `cross_unica_linha` — uma linha ÚNICA (com ou sem `flex-wrap`) usa a altura DEFINIDA do contentor mesmo quando um item a excede (antes só vencia quando era MAIOR que o maior item, o oposto do caso que o `overflow` existe para testar) ou fica aquém dela (stretch contra o contentor, não contra o item vizinho maior); `distribuir_align_content` — o `.max(0.0)` que grampeava o espaço livre saiu do ramo COM `align-content` declarado, que agora pode ficar negativo (`center`/`flex-end` sem `safe` não têm fallback por omissão: as linhas transbordam simetricamente) — só o ramo `stretch` (sem declarar) continua a grampear a 0. `coluna.rs` não mexeu: o `justify_offsets` do eixo principal da coluna já passava o `free` sem grampo nenhum. Três fixtures medidas no Edge 152 (Blink) ANTES do código. **RETRABALHO** (o build central mediu a branch já com o main — 348c4fefc —, corpus 121/125, cover 45/57, WPT flexbox 299→304 +6, mas `gap-010-ltr` que passava caiu): a causa NÃO era `flex.rs`/`flex_linhas.rs` (hipótese inicial, por serem os ficheiros que este lote reorganizou) — `gap: calc(10% - 1rem / 2)` já resolvia a 0 num worktree do commit PRÉ-merge (`a52ef68db`), confirmando um bug pré-existente. `style/lengths.rs::parse_gap_pair` separava o shorthand `gap:` com `val.split_whitespace()` cru, que os espaços DENTRO do `calc()` partem em 5 tokens inválidos — a mesma causa que `parse_edges` (mesmo ficheiro) já tinha corrigido com `split_top_ws` (paren-aware) para o espaçamento do Tailwind v4, e que `parse_gap_pair` nunca recebeu. Fix de uma linha (`split_top_ws` em vez de `split_whitespace`); `flex.rs` não mexeu. `cargo test --lib --features metrics`: 903 antes do merge → 918 com o main (outros lotes) → **919** com o retrabalho (+1 teste), 0 falhas nos três pontos. 919 testes. Corpus 122/126, **WPT flexbox 299 → 305/489** (+6: flex-align-content-center, flexbox-flex-wrap-horiz-001, flexbox-overflow-horiz-001/002, flexbox-break-request-horiz-001a/b; zero perdidos, zero encravados; retrabalho de 1 ronda (gap com calc, anterior ao lote)), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-flex-cross-size` → `bee5bec43` (2026-09-04) | `claude-flex-cross-size-overflow`, `claude-flex-wrap-stretch-linha`, `claude-align-content-center-overflow`, `claude-flex-wrap-gap-calc` |
| flex-basis-content-wrap | `flex-basis: content` (Flexbox §7.2.3) ignora `width`/`height` do item; `flex-wrap` em linha e coluna com tamanhos definidos vs indefinidos | 8 | ◐ **RETRABALHO 1 ronda** (conflito de código com o main, que tinha avançado com #2683 align/justify padding-box, #2686 DOM, e #2688 — um fecho conjunto de 5 lotes: min-auto content, baseline de contentor, aspect-ratio de substituídos, posição estática/BFC, gap): `git merge origin/main`, conflitos em `flex.rs` (o pré-passo do item passou a usar `replaced_transferido::base_e_altura_do_item`, que reintroduzia a MESMA altura-medida-pelo-contentor que a causa (2) abaixo já tinha corrigido — a correção moveu-se para dentro dessa função, no ramo SEM transferência de `<img>`), `flex_limites.rs` (dois `if` independentes no mesmo `else` — o de `content` e o novo de `largura_intrinseca_transferida` para um `<img>` transferido — ficam os dois, o de `content` depois por ser mais específico), `PLAN.md` e `tests/mod.rs` (duas listas de linhas novas, sem overlap real). Efeito lateral BOM: `flexbox-definite-sizes-003/004` (que eu tinha deixado documentado, não corrigido) **PASSAM AGORA** — o lote de posição-estática/BFC do main fechou o colapso de margem `body{overflow:hidden}`+`<p>` que era a única coisa a faltar depois do meu fix de stretch. Hash do merge: `59996442a3` (2026-09-05). `cargo test -p rts-dom --lib --features metrics`: 998 (992 do main + 6 deste lote), 0 falhas; `cargo test -p rts-dom-bridge` passa. **7 de 12 reftests do WPT fecham agora** (era 5), os outros 5 continuam documentados com a causa exata (nenhum escondido). Fecham: `flexbox-definite-sizes-003/004`, `flexbox-flex-basis-content-003a/003b`, `flexbox-flex-wrap-horiz-002`, `flexbox-flex-wrap-vert-001/002`. Causas corrigidas NESTE lote: (1) `flex_base_outer` resolvia `content` e `auto` ao MESMO `None` e caía no MESMO fallback (`child_outer_width`, que olha para `width` primeiro) — `content` nunca deve olhar para `width`; módulo novo `layout/flex_basis_content.rs`. (2) a altura CRUZADA de um item de flex-row media-se com a largura do CONTENTOR inteiro em vez da `base` (a "flex base size", Flexbox §9.2 passo 3) que o item vai ter — um `flex:0 0 content` com inline-blocks dentro de um contentor estreito multiplicava a altura por quebrar cada inline-block para a sua própria linha. (3) o max-content (`intrinsic_content_width`) de um bloco com filhos FLOAT tomava o MAIOR (tratando cada float como bloco que fecha a corrida) em vez da SOMA (como floats empacotam lado a lado com espaço infinito) — `fecha_a_corrida` muda-se para `float.rs` (já é uma distinção sobre floats) com a exceção. (4) `align-content:normal` já esticava VÁRIAS colunas de um `flex-column wrap` ao `content_w` (lote `flex-coluna-shrink`); uma coluna ÚNICA (o caso comum) ficava fora da distribuição (`columns.len() > 1`) e nunca esticava. (5) o espaço livre de grow/shrink de uma coluna usava `container_content_h` (que inclui `min-height`, correto para o `height:%` dos netos) também para decidir se os itens devem ENCOLHER — um PISO não é um TECTO; troca para `wrap_definite_h` (o mesmo limiar mais estreito que já decide o despacho do wrap). (6) `align-items:stretch` com linha ÚNICA e altura do contentor DEFINIDA só esticava para CIMA (`line_h > it.h`); a spec não tem essa condição — stretch redimensiona sempre que o item não tem cross-size própria, encolhendo se preciso (o conteúdo é que transborda do item, não o item que cresce). NÃO fechados, causa verificada e fora de âmbito deste lote: `flexbox-flex-basis-content-001a/001b` (0.096%) — o mesmo defeito na largura, mas para um `<canvas>`: `layout_canvas` lê `css.width` diretamente e nunca consulta `forced_outer_w` (pinta-se a si mesmo ANTES da imposição do flex ser lida), então um `<canvas style="width:8px">` ignora `flex-basis:content` mesmo com a base do item corrigida. `flexbox-flex-basis-content-002a/002b` (0.357%) — o espelho em COLUNA do mesmo "content ignora o próprio width/height": exige medir a altura de um item IGNORANDO a sua própria `height` declarada, que `coluna_shrink::min_main_auto` já documentava (antes deste lote) como precisando de "uma segunda passada de layout" — corte já aceite, não reaberto agora. `flexbox_flex-natural-mixed-basis-auto` (2,513%) — a distribuição de `flex-grow` confere exatamente à mão (0+97,68, 24+94, 40+94, 120+94, soma 560): a referência usa Ahem (1em por glifo) e o `ApproxMeasurer` não o emula — gap de métricas de fonte, não do algoritmo de flex. Testes novos, `layout/tests/flex_basis_content_wrap_corpus.rs` (6, um por causa corrigida). **RETRABALHO 2.ª ronda** (main avançou outra vez, #2689 flex-reverse-order): novo conflito, só em `flex.rs` — o #2689 generalizou a MESMA condição de stretch-encolhe para `(line_h - it.h).abs() > 0.5`, cobrindo o meu caso (linha única, contentor com altura definida) E o dele (`<img>` esticado cuja razão de aspeto dá altura maior do que a linha) na MESMA expressão — a minha variável `cross_definida` ficou redundante e foi removida (a condição do main já a implica: numa linha múltipla/indefinida `line_h >= it.h` sempre, por construção, então `.abs()>0.5` só dispara para CIMA, exatamente como antes; numa linha única definida pode dar `<0`, que é o encolher que eu pedia). Hash do merge: `ffe8b4efef` (2026-09-05). `cargo test -p rts-dom --lib --features metrics`: 1005 (999 do main + 6 deste lote), 0 falhas. Os 12 reftests do lote deram os MESMOS resultados de antes desta ronda (7/12 fecham, nenhuma mudança) — a generalização do main já continha o meu comportamento, não havia nada a re-conquistar. Corpus CSS/suite `*.test.ts`/paridade: por medir no fecho (regra "uma máquina") Fecho: medidos no fecho (05/09, release, ramo com main merged): corpus 141/145, **WPT flexbox 386 → 393/489** (+7 por nome: flexbox-break-request-vert-001a, flexbox-break-request-vert-001b, flexbox-flex-basis-content-003a, flexbox-flex-basis-content-003b, flexbox-flex-wrap-horiz-002, flexbox-flex-wrap-vert-001, flexbox-flex-wrap-vert-002; 0 perdidos), suite 859/888 sem perdidos, 1005 testes, paridade 6 páginas 0 movidos, cover 45/57; 2 rondas de retrabalho (merges com o main) ditas | `feat/dom-lote-flex-basis-content-wrap` → `5ce099dddb` (2026-09-05), merge 1 → `59996442a3`, merge 2 → `ffe8b4efef` (2026-09-05) | reftests do WPT (`css/css-flexbox`), autoconsistência via `claude-raster` — ver a lista de nomes acima |
| flex-align-justify-familia | a causa comum de 33 reftests do WPT (`align-items`/`align-content`/`align-self`/`justify-content`/margens `auto`), todos a falhar por 0,0–0,2 % dos pixels | 8 | ☑ causa comum: o containing block de um `position:absolute` é a PADDING BOX do ancestral (CSS 2.1 §10.1), não a border box guardada em `node_rects` — um ancestral com QUALQUER borda desviava o item 1px nos dois eixos; `layout/caixa_contentora.rs` (módulo novo) converte. Achado com `claude-paint-dump` nos dois lados de `flexbox_align-items-center.html`: item flex em (25,49), item absoluto da referência em (24,48) para o MESMO `top`/`left`. 2.º bug, isolado no 3.º item de `flexbox_align-self-stretch.html`: `height:auto` DECLARADO bloqueava `can_stretch` em `flex.rs` porque `ccss.height.is_none()` só cobre a AUSÊNCIA — `auto` é `Some(Dimension::Auto)`, não `None`; `layout/dimensao_indefinida.rs` (módulo novo) cobre os dois. Só o caminho de ROW corrigido; os mesmos `.is_none()` em `coluna_wrap.rs`/`grid.rs`/`posicionado.rs` são o corte idêntico já registado noutro lote (`grid.rs:355`, citado em `flex-justify-logico`) — fora do âmbito deste. 3.º achado, no parser: `open_implicit_body` tinha "body" na mesma lista de exclusão de "html"/"head", então um documento que escreve `<meta>`/`<title>`/`<link>`/`<style>` e depois `<body>` DIRETO (a referência de `align-items-006`) ficava sem `<html>` nenhum — `ensure_html` extraído e chamado também para "body"; reduz o desvio de `align-items-006` (1,82%→1,43%) mas não fecha (o resto é Ahem/posição estática do absolute, dois gaps maiores e fora de âmbito). **Régua do lote: 0/33 → 32/33** reftests do WPT fecham (só `align-items-006` fica, pelos dois gaps citados). `cargo test -p rts-dom --lib`: 942→**949**, 0 falhas (13 testes novos); `cargo test -p rts-dom-bridge`: 7, sem mudança. Fecho: corpus 141/145, **WPT flexbox 336 → 373/489** (+37 por nome: os 32 do lote mais `flexbox_order-abspos-space-around` e `gap-002-{lr,ltr,rl,rtl}`, que a padding box também fechou; zero perdidos, zero encravados), suite 859/888 sem perdidos, 967 testes, paridade 6 páginas 0 movidos, cover 45/57; zero rondas de retrabalho | `feat/dom-lote-flex-align-justify-familia` → `b8196cba8`+`915b566a2`+`d2354cc22`(fecho) (2026-09-05) | `caixa_contentora.rs` (3), `dimensao_indefinida.rs` (3), `flex_align_justify_familia_corpus.rs` (5), `dom/tests/parser.rs` (+2) |
| flex-gap-2 | `gap`/`row-gap`/`column-gap` em flex (Box Alignment §8): os 4 modos de escrita/direção, `column-gap` não empurra nada dentro de UMA coluna, margem `auto` vence o `justify-content` da sua coluna | 8 | ☑ dos 8 reftests do lote, `gap-002-{lr,ltr,rl,rtl}` (os 4 modos de escrita) já tinham fechado no lote `flex-align-justify-familia` (a mesma causa: padding-box do containing block de um `position:absolute`, que as referências desses 4 usam) ANTES de este lote mexer em código — verificado com a régua, não retocado. Dos 4 restantes: **`flexbox-column-row-gap-001` corrigido** (2 causas no MESMO item de `flex-flow:column wrap`, achadas com `claude-paint-dump` teste-vs-referência: (1) o `avail_w` passado a `layout_block_reusing` por um item que não estica em `coluna_wrap.rs` era `content_w` — a largura do CONTENTOR inteiro — em vez de `cw`, a largura da SUA coluna; a resolução de `margin-left:auto` de dentro do `layout_block` reabria a conta contra esse `avail_w` bem maior e empurrava o item para uma 3ª coluna a ~170px à direita da 2ª (as colunas resultantes esticavam por `align-content:normal`, escondendo a causa atrás de "3 colunas" em vez de "1 item deslocado"); (2) `Item` (`coluna_wrap.rs`) não lia `margin-top`/`margin-bottom` — corte já documentado no cabeçalho do módulo — e uma margem `auto` no eixo principal vence o `justify-content` da SUA coluna (Flexbox §8.1): sem isto a 2ª coluna recebia o mesmo `space-around` da 1ª em vez de ficar encostada ao início com o livre absorvido pela margem `auto` do último item). Fica um resíduo de 30px (1 linha da borda de UM item, <0,01 % da imagem) sob a régua ESTRITA de auto-consistência do `wpt_reftests.mjs` (exige 0 px) — a geometria bate a duas casas decimais nos dois lados (confirmado com `claude-paint-dump`), e o resíduo tem cheiro de ruído de f32 entre dois caminhos de código bem diferentes (`coluna_wrap.rs` do teste vs `coluna.rs` puro da referência, que usa DOIS flex containers separados para simular o mesmo resultado) — não perseguido, mesma classe que `0,01px em comprimentos px computados` já tolera noutra régua deste crate. **Achado de caminho, corrigido**: `column-gap` SOZINHO (sem `row-gap` nem o shorthand `gap`) era lido como substituto de `row-gap` no eixo principal de uma coluna — um `.or(css.gap)` em `coluna.rs` E `coluna_wrap.rs`, herdado do `layout.rs` pré-modularização, sem nenhum corpus ou teste do crate a exercitá-lo, e que violava a spec (Box Alignment §8: `column-gap` não tem eixo numa coluna de uma linha só). Fecha METADE de `flexbox-column-row-gap-004` (as 2 caixas SEM `writing-mode`; confirmado idêntico à referência com `claude-paint-dump`). NÃO fecham, por falta de `writing-mode` vertical — decisão de âmbito já tomada pelo lote `flex-justify-logico` ("o motor não faz layout vertical, tudo tratado como horizontal"), não revisitada aqui: `flexbox-column-row-gap-002` (as duas caixas do teste usam `vertical-lr` incondicionalmente) e a metade `vertLR` de `-004`. `gap-collapse` fica como o lote `visibility-collapse` já tinha decidido e PROVADO (medido no Edge E num Chrome real via chrome-devtools, na sessão daquele lote): o Chrome não implementa a exclusão de gap de um item `visibility:collapse` — o item colapsado continua a ocupar espaço e a comer os dois `gap`, no Chrome real e no reftest OFICIAL do WPT — e implementá-lo aqui divergiria da régua medida sem nenhuma fixture a pedi-lo; reconfirmado com a régua deste lote, não revertido nem escondido. `cargo test -p rts-dom --lib --features metrics`: 967→**969** (2 testes novos), 0 falhas. Corpus CSS/paridade/WPT completo (489)/suite `*.test.ts`: medidos no fecho conjunto vaga8-flex (05/09, release, os 5 lotes juntos sobre o main): corpus 141/145, **WPT flexbox 373 → 384/489** (+11 por nome, 0 perdidos), suite 859/888 sem perdidos, 992 testes, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-flex-gap-2` → `b79c72e24` (2026-09-05) | `flex_gap_2_corpus.rs` (2); WPT nomeados: `gap-002-{lr,ltr,rl,rtl}` (4/4, já fechados por outro lote), `flexbox-column-row-gap-001` (30px residual, era 4010px), `flexbox-column-row-gap-004` (metade fechada, era 19592px → 18356px), `flexbox-column-row-gap-002` e `gap-collapse` (fora de âmbito, ditos) |
| imagens-no-raster | o raster/paint-dump carregam PNG; `<img>` deriva a dimensão em falta pela razão e o item flex esticado transfere-a | 7 | ☑ descodificador de PNG + parse de `data:` movidos de `rts-dom-bridge` para `rts_dom::imagem` (`flate2` passa dependência REAL do `rts-dom`, sem I/O no módulo — o `std::fs::read` fica nos exemplos); `claude-raster`/`claude-paint-dump` carregam `<img src>` PNG local e `data:image/png;base64` ANTES do layout (SVG e `http(s)` continuam sem loader, dito); `layout/replaced_transferido.rs` (módulo novo — `bloco.rs`/`medida.rs` não crescem): a base/altura de um `<img>` sem tamanho, item flex esticado no eixo cruzado, vem da razão TRANSFERIDA (Flexbox §9.2) em vez do natural — cobre o item em si E o contentor que encolhe ao conteúdo (dois níveis de flex, e um contentor esticado por `position:absolute` com os dois insets); `replaced_inline_size` ganha `forced` (o tamanho que o flex já decidiu vence CSS/atributo — `<img>` nunca recebia `forced_outer_w/h` antes deste lote, o despacho em `bloco.rs` ignorava os dois). Corpus Rust `layout::tests::imagens_corpus` (3/3): as duas com razão disponível (SVG simulada por `set_pixel_data`, PNG real via o decodificador) batem os números do Blink; a terceira (imagem PARTIDA sem `alt`) fixa o que o motor faz hoje (altura 0) — o ícone de fallback do Chromium (16, medido) não é uma constante de CSS2 §10.3.2/§10.6.2 nem nada que este motor calcule, corte dito. Corte: só flex-ROW (eixo cruzado = altura); flex-COLUMN esticado na largura fica por fazer. **RETRABALHO 1 ronda** (o orquestrador mediu a branch com o main já mesclado — `cc41371f4` — e reportou WPT flexbox 335→328, 7 reftests que passavam a cair, 0 dos 8 esperados a fechar): os 7 usavam um `<img>` com razão de aspeto cuja caixa nunca tinha sido COMPARADA por pixel antes (mascarada — a régua via 0 diferenças por baixo da máscara, mesmo com a caixa errada); ao carregar PNG de verdade, o defeito PRÉ-EXISTENTE ficou visível: `table::min_content`/`coluna_shrink::min_main_auto` (o piso automático de `min-width`/`min-height:auto`, Flexbox §4.5) media um replaced com as duas dimensões declaradas pela DECLARADA (via `replaced_inline_size`, certo para a caixa real, errado para este piso — o candidato (d), "a dimensão derivada da razão", nunca era calculado), e o pré-passo de flex.rs perguntava a `table::min_content` (que não sabe de eixo cruzado) mesmo quando o próprio transferido já tinha a resposta certa, erguendo-o de volta ao natural. Fix: `largura_min_content_por_razao`/`altura_min_content_por_razao` (novas, `inline_box/substituido.rs`, ao lado de `ratio_do_replaced` — uma implementação, os dois eixos chamam-na) computam o candidato (d) com o clamp de `max-height`/`min-height` (CSS2 §10.7); `base_e_altura_do_item` devolve agora `transferiu: bool`, e quando `true` o pré-passo usa a própria base transferida como `min_main`. Corpus Rust +3 testes (`layout::tests::imagens_corpus`, 6/6). `flexbox-min-width-auto-002a/b/c` e `flexbox-definite-cross-size-constrained-percentage` batem a referência rect a rect (confirmado com `claude-paint-dump` contra os ficheiros reais do WPT); `flexbox-min-height-auto-002a` também, mas `-002b`/`-002c` continuam com um resíduo: a IMAGEM já mede certo (34×34) mas o CONTENTOR `.flexbox` (float, shrink-to-fit) usa `intrinsic_outer_width` (`medida.rs`), que devolve a `width` do filho sem aplicar `max-width`/`min-width` — um bug PRÉ-EXISTENTE, genérico (não é de imagens), fora de âmbito deste lote (precisa da sua própria régua). `cargo test -p rts-dom --lib --features metrics` 943→946, 0 falhas; `cargo test -p rts-dom-bridge` 4, 0 falhas; 954 testes. Corpus 141/145, **WPT flexbox 336 → 336/489** (+0 líquido: os 2 do retrabalho, flexbox-min-height-auto-002b/c, voltaram; zero perdidos por nome, zero encravados; 2 rondas de retrabalho (régua do PNG medida com a imagem partida, SVG data: por atributos, guard is_column)), suite 859/888 sem perdidos, paridade 6 páginas 0 movidos, cover 45/57; dois merges de `origin/main` resolvidos e re-medidos (pintura-e-caixas/flex-basis-piso/pequenos-1/flex-coluna-shrink/AOT em `a765119b6`; flex-cross-size/visibility/flex-justify-logico/flex-min-content/flex-column-wrap em `cc41371f4`). **RETRABALHO 2 ronda** (fecha o resíduo de `-002b`/`-002c` e os itens 2/3 pedidos junto): o resíduo tinha DUAS causas, não uma — `largura_intrinseca_transferida` (`replaced_transferido.rs`) não distinguia `flex-direction:column` de row (nada em `css_display`/o display computado carregava essa distinção) e disparava a transferência TAMBÉM no `.flexbox` em coluna, que não devia participar; fechado com um guard `is_column` logo no topo (`None` se `flex_direction` for coluna, ou se o display não for `DISPLAY_HORIZONTAL`/`DISPLAY_WRAP`). Por trás dela um bug de borda a CONTAR DUAS VEZES em `intrinsic_content_width` (`medida.rs`): devolvia a caixa COM borda de `replaced_inline_size` como se já fosse conteúdo — corrigido subtraindo `border-left`/`border-right`. `intrinsic_outer_width` ganhou o clamp `min-width`/`max-width` que faltava (CSS2 §10.4); o MESMO clamp em `child_outer_width` quebrava `flex_base_size_nao_capada_por_max_width_e_encolhimento_congela` (Flexbox §9.2 passo 3 proíbe capar a base flex ali) — por isso o clamp de um float fica no site de chamada (`vertical.rs::layout_children_vertical`, deslocado pela margem para não subtrair a margem duas vezes), não dentro de `child_outer_width`. Item 2 (o orquestrador commitou `tests/css/support/20x50-green.png`, 84 bytes): a fixture usa paleta de 1 bit por píxel, que o decodificador rejeitava (só aceitava 8 bits/canal) — `png.rs` ganha o ramo `sub_byte` para os tipos 0/3 em 1/2/4 bits (PNG §11.2.2), com o `bpp` da previsão dos filtros a nunca descer de 1 byte mesmo sub-byte (§9.2), sem tocar no caminho de 8 bits já testado; `.esperado.json` foi RE-medido pelo orquestrador com o PNG presente (90×225, era 90×16 — o ícone de imagem partida da 1ª medição sem o ficheiro). Item 3: `Dom::image_dims` ganha `svg_data_url_dims` (`dom/formulario.rs`) — lê `width`/`height`/`viewBox` da raiz de um `data:image/svg+xml,` por texto, sem descodificar nem rasterizar (este motor não tem descodificador de SVG); fecha `claude-img-sem-tamanho-natural-em-flex.html` (100×100 nos 3 elementos, sai de `esperado-a-falhar.txt`). Corpus Rust `layout::tests::imagens_corpus` reescrito para usar a SVG REAL em vez de `set_pixel_data` simulado, 6/6. `cargo test -p rts-dom --lib --features metrics` 946→954, 0 falhas; `cargo test -p rts-dom-bridge` 4, 0 falhas. Confirmado por `claude-paint-dump` rect a rect: `flexbox-min-height-auto-002a/b/c`, `flexbox-min-width-auto-002a/b/c`, `flexbox-definite-cross-size-constrained-percentage` (os 7 do RETRABALHO 1, todos fechados agora) e as duas fixtures. NÃO verificado: `examples/claude-css-runner.ts` via `run_fixture` (a ponte real TS/`rts-dom-bridge`) crasha com `TypeError: Cannot read properties of undefined (reading '@@iterator')` em QUALQUER fixture — inclusive uma que já passava antes deste lote (`img-natural`) — pré-existente e fora de âmbito; item 2 fica confirmado só pelos outros dois caminhos (teste unitário do PNG real do disco + `claude-paint-dump`). | `feat/dom-lote-imagens-no-raster` → `4a9446523`+`d5899e14d`+`6df172168`+`0aec3df12`+`36da674c5`+`a765119b6`+`cc41371f4`+`cf4dcc420`+`652fddc0b`+`2983cca19`+`0bcd7f438`+`6f3d23c21`(fecho) (2026-09-05) | `layout::tests::imagens_corpus` (6/6); `claude-img-aspect-ratio-sem-loader`/`claude-img-sem-tamanho-natural-em-flex` no corpus CSS; WPT flexbox `flexbox-min-width-auto-002*`/`flexbox-min-height-auto-002*`/`flexbox-definite-cross-size-constrained-percentage` |
| flex-reverse-order | `row-reverse`/`column-reverse` com `wrap`/`wrap-reverse` (ordem das linhas E dos itens, `align-content` com linhas invertidas), `order` com `direction:rtl`, `order` com `position:absolute`+`space-around`, a família `css-flexbox-row*` | 8 | ◐ duas causas pequenas e independentes, réguas medidas nos 11 reftests do lote ANTES do código. (1) `coluna_wrap.rs`: a ORDEM DAS COLUNAS de um `flex-direction:column`+`flex-wrap` sob `direction:rtl` inverte do MESMO jeito que `wrap-reverse` (Flexbox §4.1 + Writing Modes: as duas trocam o par cross-start/cross-end) — por isso CANCELAM quando presentes juntas (`wrap_reverse ^ rtl_horizontal`); antes, `direction:rtl` sozinho não tinha efeito nenhum na ordem física das colunas (só no espelho de um item DENTRO da sua própria coluna, `coluna_rtl::cross_x`, já correcto antes deste lote). Fecha `flexbox_rtl-order` (WPT, referência hand-authored por floats reordenados). (2) `flex.rs`: `align-items:stretch` só esticava um item cuja `it.h` pré-stretch já era MENOR do que a linha (`line_h > it.h`) — nunca ENCOLHIA um item cuja altura veio maior pela razão de aspecto de um `<img width>` sem `height`; `(line_h - it.h).abs() > 0.5` corrige as duas direcções (Flexbox §9.4 passo 7 não distingue crescer de encolher). Confirmado seguro contra o teste pinado `linha_unica_sem_wrap_usa_a_altura_do_contentor_mesmo_com_item_maior` (`flex_cross_size_corpus.rs`): o item alto ali declara a sua PRÓPRIA altura, então `can_stretch=false` continua a bloqueá-lo — o corte só muda para um item SEM `height` declarado. Melhora `css-flexbox-img-expand-evenly` de 2,61 % para 1,14 % (largura E altura do flex batem agora exactamente a referência) mas NÃO fecha: o resíduo é a REFERÊNCIA em si, cujas 3 `<img>` (fora de um flex, alinhamento `baseline` normal) saem a y=32,4 — ACIMA do topo do seu próprio contentor — um bug pré-existente e distinto (baseline de um elemento substituído muito mais alto do que a linha de texto ao lado, a mesma lacuna que a auditoria de 2026-09-04 já registava: "sem baseline/ascent/descent por átomo de linha"), fora do tema deste lote. Fecha, sem ter sido medido nos 11 reftests do lote (achado pela régua CHEIA do WPT, não pela lista): `flexbox-single-line-clamp-1` ("single-line flex containers should clamp their line's height to the container's computed min AND max cross-size" — exactamente o mesmo passo 7 do §9.4, um `height:400` contra um `max-height:200` do contentor). NÃO tentado, com o porquê: os 6 `css-flexbox-row*` e o `css-flexbox-test1` precisam de `writing-mode:vertical-rl` real — corte de arquitectura já documentado em vários lotes ("o motor não faz layout vertical, tudo tratado como horizontal") e fora do alcance de um lote sobre ordem/reverse; `flex-column-reverse-multiline-item-position` precisa de separar DOIS valores hoje fundidos num só parâmetro — o limiar de wrap de `max-height` e a altura AUTO finalmente resolvida (o maior das linhas), em `coluna_wrap.rs`/`bloco.rs` — mudança de algoritmo que arrisca todo container de coluna com altura definida, não um ajuste local; `flexbox_order-abspos-space-around` já tinha fechado por #2683 (confirmado ainda a passar, intocado). **RETRABALHO (o build central mediu a branch: `gap-007-rtl` a cair 0,21 % — "não faço merge com um reftest a cair").** A CAUSA: `style/logical.rs` resolvia `margin-inline-end` sempre como `margin-right` fixo, ignorando `direction` — a ordem de colunas de (1), agora correcta, desmascarou o lado errado que uma ordem trocada por engano escondia por coincidência. FECHADO, não só declarado: (a) `style::parse::caixa` tinha um braço PRÓPRIO para `margin-inline-start/end`/`padding-inline-start/end` que respondia ANTES de `style::logical::try_apply` na cadeia de `aplica_declaracao` — DUAS traduções do MESMO nome, e a de `caixa.rs`, sempre LTR, escondia a de `logical.rs` por completo (tornar esta última direction-aware não mudava nada até se remover a outra). Removido de `caixa.rs`; as duas famílias reentregam a `style::logical` como `border-inline-*`/`inset-inline-*` já faziam. (b) Isso só chega ao valor CERTO se resolver por ELEMENTO, não por REGRA: uma regra CSS compila-se uma vez, partilhada por todo elemento que a casar, e não sabe nesse momento qual vai ser o `direction` herdado por cada um — `margin-inline-end` e as suas quatro famílias (margin/padding/border-inline-\*, mais `inset-inline-*` de graça, pela mesma função) passam a ficar PENDENTES (`style::parse::apply_specified_declaration` reusa a fila que já existia para `var()`; `apply_declaration_final`, novo, é a parte de baixo que `style::stylesheet::apply_resolved_decl` chama directo — chamar `apply_specified_declaration` reclassificaria a declaração já resolvida e a perderia em silêncio) e só resolvem em `dom::cascade::computed_style_idx_inner`, contra o `direction` já herdado (calculado cedo, antes da herança oficial correr — a mesma razão de `parent_font` no `font-size`) ou já declarado pelo próprio elemento (a precedência normal da cascade, preservada por um `.or`). `style::parse::parse_inline` (usado por vários testes sem árvore) resolve as pendentes contra um contexto vazio — `direction` cai no inicial, o mesmo que sempre respondeu. (c) Isto por si só regredia DOIS reftests que só passavam por uma coincidência inversa: `gap-003-rtl`/`gap-006-rtl` (linhas de flex RTL que simulam um `gap` com `margin-inline-end`) pioravam de 0,31 %/0,16 % para 4,86 %/1,05 % — margem agora certa, mas a ORDEM DOS ITENS da linha continua sem inverter por `direction` (só por `row-reverse`; reverter os itens ANTES do agrupamento em `flex.rs`, tentado e revertido, muda QUAIS itens partilham linha sob `flex-wrap` quando margens por item os distinguem — a mesma classe de bug que `coluna_wrap.rs` já resolvera para colunas, aqui ainda por fazer para linhas). `dom::direction_herdada` (módulo novo) fecha o gap SEM tentar essa reordenação: nega o `direction` herdado como fallback quando o pai é uma LINHA de flex (`row`/`row-reverse`, não coluna), devolvendo exactamente o resultado LTR que já existia para essa combinação — as duas fixtures voltam a 0 %. Corpus/suite `*.test.ts`: medidos no fecho (05/09, release, ramo com main merged): corpus 141/145, **WPT flexbox 384 → 386/489** (+2 por nome: flexbox_rtl-order, flexbox-single-line-clamp-1; 0 perdidos, gap-007-rtl de volta), suite 859/888 sem perdidos, 999 testes, paridade 6 páginas 0 movidos, cover 45/57; 1 ronda de retrabalho (lógicas por elemento) dita. `cargo test -p rts-dom --lib --features metrics`: 970→**974** (+4: 3 lógicas LTR/RTL/corte-de-linha + 1 de coluna sem o corte), 0 falhas. **WPT flexbox (medido localmente com `claude-raster`/`claude-paint-dump` em debug, não o binário do fecho, os 489 completos, duas medições independentes com o mesmo resultado): 374 → 377/489** — por ficheiro: +1 `gap-007-rtl` (de volta), zero perdidos (`gap-003-rtl`/`gap-006-rtl` continuam a passar); a 374→377 soma 3, não 1, porque a medição de 374 tinha sido feita durante uma reconstrução concorrente do binário (corrida de build) e por coincidência reportou `flex-abspos-inset-nested-001`/`-002` como falha quando já passavam desde (2) — confirmado isolando o retrabalho por `git stash` e medindo os dois pontos de novo, limpos: **373 (o início do lote) → 377/489, +4 ao todo (`flexbox_rtl-order`, `flexbox-single-line-clamp-1`, `flex-abspos-inset-nested-001`/`-002` — estes três por (1)/(2); `gap-007-rtl` por este retrabalho), zero perdidos em qualquer dos dois pontos de comparação** | `feat/dom-lote-flex-reverse-order` → `a644bfee5`+`2f7fd4fa5` (2026-09-05) | `flex_reverse_order_corpus.rs` (7/7) |
| flex-min-auto-content | `min-width`/`min-height:auto` (mínimo automático, Flexbox §4.5) num item flex, nos dois eixos; `min-width`/`max-width: max-content`/`min-content` num item flex | 8 | ☑ parcial — 4 de 12 reftests fecham, 2 melhoram sem fechar (resíduo sub-pixel de um `calc()`, instrumento), 6 ficam por triagem/causa fora de âmbito, ditos abaixo. Régua ANTES do código (os 12 corridos no `feat/dom-lote-flex-min-auto-content` origem: todos falhavam). Módulo novo `layout/intrinseco_min_max.rs` (`resolve`, partilhado por `bloco.rs` — só a CHAMADA, 2 linhas trocadas, 0 de delta — e `flex_limites::limites_do_item`, que deixa de duplicar o próprio `match`): `min-content`/`max-content` como valor de `min-width`/`max-width` resolvem para a intrínseca REAL (`table::min_content`/`content_natural_width`) em vez do `None` que `Dimension::resolve` dá a uma keyword que só a árvore resolve — antes só um sítio entendia `MinContent`, e só para a largura do PRÓPRIO item flex. `flex_limites::min_automatico` (eixo de linha): o `max-width` COMPUTADO entra no `min()` do automático (candidato (b) da spec) em vez de só clampar o resultado FINAL — sem isto um item com `max-width` menor do que o conteúdo saía no conteúdo, não no tecto (`com_limites_finais` faz `min` antes do `max`, e um `min_main` maior do que `max_main` vencia sempre); ganha também o desligamento por `overflow-x`/`overflow-y` não visível (CSS Overflow 3: um eixo visível computa como não-visível quando o OUTRO não é). `coluna_shrink::min_main_auto` (eixo de coluna, espelho): com `height` declarado e SEM razão de aspeto, era 0 sempre (corte antigo, dito no próprio cabeçalho) — agora é o menor entre `height` (specified) e o conteúdo dos FILHOS ignorando esse `height` (`altura_conteudo_sem_height`, nova: soma `child_outer_height` por filho, sem uma 2ª passada de `layout_block` completa no item). `flex.rs`/`coluna.rs`/`coluna_wrap.rs` só ganham o parâmetro extra nas chamadas. Testes novos: `layout/tests/flex_min_auto_content_corpus.rs`, 6 (todos passam à primeira, geometria exata prevista à mão). `cargo test -p rts-dom --lib --features metrics`: 954→**960** (+6), 0 falhas; `cargo test -p rts-dom-bridge`: 4, 0 falhas — sem regressões nas duas suites. **Reftests do lote (12, régua antes/depois por nome):** `flexbox-min-width-auto-003`/`-004` FECHAM (0 px de diferença — overflow desliga o automático, com e sem a promoção `overflow-y`→`overflow-x`). `flexbox-min-width-auto-001` e `flexbox-min-height-auto-001` ficam a 4 px de diferença (~0,0004%) de fechar — os 8/9 blocos de cada um já bateram a régua rect a rect (`claude-paint-dump`); o resíduo é o bloco do `calc(10%+50px)`, que resolve a 54,1 (matematicamente correto: 10%×1px+50) onde a fixture do WPT arredonda a 54 — um artefacto de sub-pixel do rasterizador, não um erro de `min-width`/`min-height:auto`; não "consertado" alterando a fixture nem a resolução do `calc()`. `flex-item-content-is-min-width-max-content` melhora (0,126%→0,081% de pixels diferentes) mas não fecha: o `min-width:max-content` do PRÓPRIO item flex já funciona (testado no corpus novo), mas um `min-width:max-content` num DESCENDENTE não-flex (o caso exacto do WPT) exige que `table::widths::min_content` — a recursão que dá o piso de min-content a QUALQUER ancestral, incluindo um item flex — respeite o `min-width` de CADA nó visitado, e esse ficheiro está EXACTAMENTE no tecto de 500 linhas; a mudança mínima teria de tocar a linha `declarada` já testada (risco de regressão num ficheiro central a toda a tabela) ou crescer o ficheiro (viola o tecto) — corte dito, fica por fazer com a causa exacta apontada. `flexbox-min-width-auto-005`/`-006`: a parte deste lote (tamanho do `<img>` via razão transferida, já do lote imagens-no-raster) já bate; o resíduo é de OUTRA causa em cada um — `-005` é a posição ESTÁTICA de uma caixa `position:absolute` sem `top`/`left` (sai em (0,0) em vez de onde cairia no fluxo — o motor não implementa "static position"), `-006` é a falta de um descodificador de SVG (uma das duas imagens do teste é `data:image/svg+xml`, que este motor só dimensiona por texto, nunca pinta) — nenhuma das duas é `min-width`/`min-height:auto`, ambas fora de âmbito e cada uma precisaria da sua própria régua. `flexbox_stf-table-singleline`/`-2`: a MESMA célula de tabela com um flex `width:0` lá dentro que o lote `flex-min-content` já tinha marcado como corte ("a 2ª metade da causa min-intrinsic... cell_min_max ramo fixo... sem fixture" — a fixture aqui É essa); a causa é a mesma de cima (`table::widths::min_content`, tecto de 500), mais funda: uma célula com conteúdo de largura FIXA (`width:0`) precisa que o piso de min-content da célula considere o automático dos FLEX ITEMS lá dentro, não a largura fixa nua — mesmo ficheiro, mesmo tecto, mesmo corte. `multiline-shrink-to-fit`: shrink-to-fit de um flex-COLUMN com `flex-wrap` flutuado — o corte que `medida.rs` já assinala ("a multi-coluna do wrap é corte dito"); tentei derivar as colunas à mão a partir da marcação e o resultado não bateu com o `.esperado` do WPT (uma leitura de "2 colunas de 100 = 200px" onde a régua espera 100px num dos casos), o que quer dizer que o meu modelo do algoritmo de intrinsic sizing multi-linha (Flexbox §9.9) está errado nalgum ponto que só a spec ao pormenor resolve — não implementado por falta de confiança na leitura, não por falta de tempo. `flex-container-max-content-001`/`-min-content-001`: cada um é uma matriz de ~48 sub-casos (`{row|col}×{flexible|no-grow|no-shrink}×{sem|pequeno|grande} pref×{harmonioso|disjunto}`); o `intrinsic_content_width` do container soma a largura NATURAL de cada item (não a "contribuição max/min-content" da spec, que depende de `flex-grow`/`flex-shrink`/`flex-basis` do próprio item, Flexbox §9.9) — a diferença aparece em ~1/3 dos sub-casos (95/144 e ~90/144 caixas com w/h diferentes da referência, medido por `claude-paint-dump`), sem UMA causa dominante isolável no tempo do lote; implementar o algoritmo §9.9 inteiro é o trabalho, não uma correcção pontual, e mexer em `intrinsic_content_width` sem essa régua arrisca as fixtures que já passam por ele hoje. Corpus CSS/paridade/suite `*.test.ts`: medidos no fecho conjunto vaga8-flex (05/09, release, os 5 lotes juntos sobre o main): corpus 141/145, **WPT flexbox 373 → 384/489** (+11 por nome, 0 perdidos), suite 859/888 sem perdidos, 992 testes, paridade 6 páginas 0 movidos, cover 45/57 (uma máquina). **RETRABALHO 1 ronda** (o orquestrador mediu o fecho conjunto — este branch + gap + baseline-2 sobre o main — e reportou `min-size-auto-overflow-clip` a cair, que passava no main): confirmado isolado neste branch (`claude-raster` teste-vs-referência: 0,24% com os ficheiros do lote, 0% revertendo-os) — causa própria. `Overflow` (`scrollbar.rs`) fundia `hidden`/`clip` numa SÓ variante (`Hidden`), então o desligamento por overflow não-visível que este lote deu a `min_automatico` tratava `clip` como `hidden` — mas a spec (Flexbox §4.5, e o próprio título do WPT: "overflow: visible and clip behave the same for min-size purposes") trata `clip` como `visible` aqui: `clip` não é scroll container (CSS Overflow 3 §clip, não rola nem por script), só um eixo que PODE rolar desliga o automático. Fix: `Overflow` ganha a variante `Clip`, separada de `Hidden`; dois métodos novos — `clips()` (hidden|clip) ao lado do `scrollable()` (auto|scroll) já existente — para os três sítios que dependiam da fusão sem saber (`bloco.rs::overflow_bfc`, CSS2.1 §9.4.1 — reescrito com os dois métodos, 10→3 linhas, o ficheiro ENCOLHE; `bloco.rs::scrolls_x`; `segmento.rs::elipse_pedida`, `text-overflow:ellipsis`), mais o arm que faltava em `fmt_values.rs` (achado pelo `match` exaustivo do COMPILADOR, não por leitura) e o mesmo desligamento em `coluna_shrink::min_main_auto` (eixo de coluna, sem fixture ainda mas a MESMA pergunta — ficava inconsistente ao lado do fix da linha). Rejeitado: `rts-egui/frame/mod.rs:339` (`scroll_y` interativo) — exigiria RULE 0 de `docs/ui/egui-crate.md` para uma nuance sem teste automatizado nenhum; dito, não corrigido. Os 12 do lote: SEM MUDANÇA (nenhum usa `overflow:clip`). `min-size-auto-overflow-clip`: 0,24%→**0%**. `flexbox-min-height-auto-002b/002c` (fora do lote): reconfirmados a 0%. `cargo check -p rts-dom -p rts-dom-bridge -p rts-egui`: limpo. `cargo test -p rts-dom --lib --features metrics`: 960, 0 falhas; `-p rts-dom-bridge`: 4, 0 falhas; `-p rts-egui`: 6, 0 falhas — sem regressões nas três. | `feat/dom-lote-flex-min-auto-content` → `8d024f477`+`ba8fa19ee`+`eb27a32cf` (2026-09-05) | `layout::tests::flex_min_auto_content_corpus` (6) |
| flex-baseline-2 | a baseline de um CONTENTOR flex vista de FORA (Flexbox §8.5) | 8 | ☑ `flex_baseline::ascent_do_contentor` (novo): quando o nó que `linha_ib::ascent_do_item` mede é ele próprio flex/inline-flex, a baseline não vem da fonte do CONTENTOR (a fórmula genérica — certa só para um bloco comum, errada aqui: um flex tem baseline por DEFINIÇÃO da spec, não da sua própria fonte) — vem do GRUPO baseline da 1ª linha quando algum item participa (a MESMA pergunta que `calcula_linha` já fazia para o alinhamento INTERNO, só que agora também para o que se vê de FORA; e só no eixo de LINHA — uma coluna nunca participa, Flexbox §8.5, a mesma leitura que já faz `coluna.rs::align_offset` cair em `FlexStart`), senão do PRIMEIRO item em fluxo — `ascent_do_item_neto`, RECURSIVO porque esse item pode ser OUTRO flex (um flex dentro de outro dentro de um `inline-flex`, propagado até ao fim); sem item nenhum, devolve `h` tal e qual (§8.5 sintetiza do bottom margin edge, que é o que `h` já significa). Calculado por ESTRUTURA (margem própria + recursão), sem reexecutar o algoritmo de flex do filho — corte declarado no ficheiro: o ascent de um item de conteúdo normal (ancorado ao topo da própria caixa) não muda com o `stretch` do eixo cruzado, só a margem e a fonte decidem. Verificado por 3 testes Rust NOVOS que FALHAM sem o fix e PASSAM com ele (confirmado por `git stash`/`pop` nos dois ficheiros de produção): flex aninhado propaga a baseline do GRUPO interno para o contentor exterior; sem participante interno (align-items ≠ baseline), cai no PRIMEIRO item, ignorando os outros; numa COLUNA com `align-items:baseline` DECLARADO no filho, ainda cai no primeiro item — a propriedade não tem efeito INTERNO em coluna, mas isso não muda a regra vista de FORA. **RETRABALHO** (o fecho conjunto com os lotes `gap`/`min-auto` sobre o main derrubou o WPT `flex-order-wrap-reverse-baseline`, que passava em `main`; confirmado no branch com `claude-raster`: PASS em `6e37093ea`, FAIL aqui, 960px de diferença): a causa era ler os filhos pela ordem do DOM crua — `order` decide a ordem de FLEX, não a do DOM, e `flex-wrap: wrap-reverse` inverte a ordem das LINHAS no eixo cruzado (Flexbox §8.3, a mesma leitura que `reverte_linhas_se_wrap_reverse` já aplicava às linhas REAIS de `flex.rs`) — a linha que o documento escreve DEPOIS é a "primeira" para a baseline (§8.5), e as duas regras acima passaram a operar só sobre ELA. `em_ordem_de_flex` (ordena por `order`, estável) + `linhas_por_largura` (novo — agrupa em linhas pela largura DECLARADA, aproximação sem grow/shrink/gap, só para saber a que linha cada item pertence; inverte-se sob `wrap-reverse`). 2 testes novos (`order` sem `flex-wrap`; a linha do `wrap-reverse` desenhada primeiro) FALHAM sem o commit do retrabalho, confirmado por `git checkout --`/reaplicar o diff; os 2 WPT já fechados que usam `order`+baseline (`flex-order-last-baseline{,-multiple}`) continuam a passar (não exercitam este caminho: `align-items:last baseline` nunca participa do grupo). `flex-order-wrap-reverse-baseline` confirmado PASS depois do fix (`claude-raster`, 0px). Os 10 reftests do lote: re-medidos, número IDÊNTICO ao de antes do retrabalho. **Os 10 reftests do WPT do lote continuam a falhar — MEDIDO antes e depois da mudança, os DEZ com o MESMO número de pixels — por causas ALHEIAS a este mecanismo, nenhuma tocada aqui, cada uma confirmada por leitura ou sonda**: (1) os 6 `flexbox-baseline-align-self-baseline-{horiz,vert}-001` / `flexbox-baseline-multi-item-{horiz,vert}-001{a,b}` têm DOIS motivos independentes, cada um por si já suficiente. Primeiro: a referência usa `.flexContainer > * {display:inline}` com `font-size` DIFERENTE por filho para imitar visualmente a versão flex, e `runs.rs::InlineRun` não carrega um `font-size` por run — só cor/peso/decoração/itálico (confirmado com uma sonda isolada: um `<span style="font-size:40px">` dentro de um contentor de `font-size:10px` pinta `"size":10` na `DisplayList`, não 40). Segundo, e mais fundo: mesmo que a referência estivesse certa, o LADO DO TESTE (`.flexContainer` como filho de `<body>` ao lado do texto solto `"a"`) nunca chega a chamar `ascent_do_item` — `caixa.rs::em_contexto_inline` vê `"a"` como irmão de texto não-vazio e desvia o grupo inteiro para `linha.rs::layout_inline_flow` em vez de `linha_ib.rs::layout_inline_block_line` (o corredor que ESTE lote corrige), e o ramo `AtomicKind::Block` de `linha.rs` planta todo átomo não-vazio em `cy` fixo, incondicionalmente — nunca pergunta a baseline de ninguém. Confirmado com `claude-paint-dump`: os 3 `.flexContainer` de `align-self-baseline-horiz-001` saem TODOS em y=0 apesar de terem grupos baseline internos diferentes (10px/20px/35px por contentor), a assinatura exacta de "mesma posição para todos" que um `cy` uniforme produz. Ensinar `linha.rs` a chamar `ascent_do_item` nos seus átomos é um lote À PARTE — o mesmo ficheiro que documenta esta escolha (`caixa.rs`, no cabeçalho) já regista a MESMA classe de defeito ("5 perguntas iguais, cada correção só chega a uma") como mudança de fundo para medir em separado, não uma linha a acrescentar aqui. (2) `flexbox-collapsed-item-baseline-001` já saiu de âmbito no lote `visibility-collapse` — medido no Edge E num Chrome real na mesma sessão, o PRÓPRIO Chrome não implementa esta parte da spec (`visibility:collapse` a contribuir para o cross-size via baseline), então fechá-lo aqui divergiria da régua que os outros lotes usam. (3) a referência de `multiline-reverse-wrap-baseline` usa uma `<table>` com `vertical-align:bottom` já apontada como bug de TABELA (não de flex) pelo lote `flex-baseline`, não tocada por nenhum dos dois lotes. (4) os dois `dynamic-baseline-change{,-nested}` dependem de um scrollbar REAL a reduzir o content-box de um `overflow:auto` (o canvas empurra o `#target`) — este motor só DESENHA a scrollbar, nunca a faz ocupar espaço (`docs/ui/html-engine/analises/2026-09-04-auditoria-estrutural/README.md`, achado 3: `scrollbar.rs` só desenhado). Tentado o caminho honesto pedido no brief (`run_fixture`+`runScripts`, em vez de rasterizar sem JS como o `wpt_reftests.mjs` já faz) — `crates/rts-host` não tinha binário nesta árvore e o primeiro `cargo run -p rts-host --example run_fixture` compila o motor inteiro do zero; NÃO terminou a tempo de entrar nesta linha com um número, e não há ATALHO honesto para isso sem ferir "uma máquina" (não construir o motor completo num agente). `cargo test -p rts-dom --lib --features metrics`: 972 (967+5, retrabalho de 1 ronda), 0 falhas. Corpus/WPT/suite: medidos no fecho conjunto vaga8-flex (05/09, release, os 5 lotes juntos sobre o main): corpus 141/145, **WPT flexbox 373 → 384/489** (+11 por nome, 0 perdidos), suite 859/888 sem perdidos, 992 testes, paridade 6 páginas 0 movidos, cover 45/57 | `feat/dom-lote-flex-baseline-2` → `b8b2075b4` (2026-09-05) | `flex_baseline_2_corpus.rs` (5/5) |
| flex-aspect-ratio-collapse | aspect-ratio/tamanho intrínseco de substituídos em coluna (o corte deixado por `imagens-no-raster`); `visibility:collapse` horizontal/wrap FICA DE FORA, medido | 8 | ◐ **Régua ANTES do código**: 14 reftests do WPT medidos por nome via `claude-raster`, todos a falhar (WPT flexbox 375/489 no checkout local antes de qualquer mudança — 2 acima do 373/489 do fecho anterior; não investigado, dito em vez de silenciado: pode ser deriva do checkout WPT, não fixado a um commit aqui). Achado antes de mexer: só 2 dos 14 (`-resize-001`, `-image-load`) foram citados como tendo `<script>`, mas **5 têm** (`aspect-ratio-intrinsic-size-012/013` e `-percentage-height-stale-width` também mudam o DOM depois do layout). Feito: (1) `<img>` com `src` mas SEM ratio nenhum (`data:image/svg+xml` sem `width`/`height`/`viewBox`) cai no default de CSS Images §5 (300×150) em vez de `(0,0)` — só com `src`: `imagem_sem_dimensao_nenhuma_continua_sem_caixa` fixa que SEM `src` continua `(0,0)`, e uma 1ª versão sem esse guard regredia esse teste (`cargo test` apanhou, revertido); (2) `coluna.rs` passa `forced_outer_w` a um `<img>` esticado (`align-items:stretch`, default) numa coluna flex — antes só o eixo ROW tinha isto (a lacuna que `imagens-no-raster` já tinha deixado escrita: "flex-COLUMN esticado na largura fica por fazer"); as duas juntas fecham `flex-svg-no-intrinsic-column-001`. (3) `<img>` ganha PADDING na sua caixa e na razão (`substituido.rs`/`replaced.rs`): a altura deriva-se do CONTENT-BOX (não do border-box) e só depois soma o padding — `flex-aspect-ratio-intrinsic-padding-001` bate 240×140 exacto por `claude-paint-dump`, mas o RASTER continua a acusar diferença: o motor não rasteriza `data:image/svg+xml` nenhum (dito no cabeçalho do `claude-raster.rs`), então o lado nosso pinta 0 itens onde a referência pinta um retângulo verde — instrumento, não geometria (verificado, não escondido). NÃO feito, com a causa: `flexbox-collapsed-item-horiz-001/002/003` e as duas `flexbox_visibility-collapse*` pedem o "strut" de Flexbox §4.4 (main size 0, cross size da linha PRÉ-colapso) — implementado e testado à parte (revertido antes do commit): fecha os 5 reftests do WPT mas REGRIDE os 3 corpus `tests/css/claude-*visibility-collapse*`, medidos contra Edge 152 REAL em 2026-09-04, que mostram um item colapsado a manter a largura CHEIA no eixo principal, os DOIS gaps e o `flex-grow` — sem strut nenhum. A régua deste repositório é o Chrome (`CLAUDE.md`), não a auto-consistência do WPT; implementar quebraria 3 fixtures medidas para fechar 5 de auto-consistência, a MESMA razão pela qual `gap-collapse` já tinha sido rejeitado por outro lote — só que aqui o corte é o algoritmo inteiro, não só o `gap`. `aspect-ratio-intrinsic-size-007` fica por uma capacidade que não existe: SVG EXTERNO (`support/x.svg`, não `data:`) não tem leitor de dimensão nenhum (`svg_data_url_dims` só lê `data:image/svg+xml,`) — precisa de um `Dom::set_svg_dims`/equivalente e um loader nos dois exemplos, fora de âmbito de um fix rápido. `aspect-ratio-intrinsic-size-012/013` e `-percentage-height-stale-width` ficam por uma lacuna mais geral: a propriedade CSS `aspect-ratio` só deriva ALTURA a partir da LARGURA (`bloco.rs:673`) — nunca o inverso (largura a partir de uma altura FORÇADA/esticada) para um elemento QUALQUER, só para `<img>` via `replaced_transferido.rs`; confirmado com `run_fixture`+`getBoundingClientRect()` (script `target/reftest-scratch/ts/harness.ts`, não commitado): o `<div aspect-ratio:2/1>` esticado a 50px de altura por um flex-row mede LARGURA 0, devia medir 100. `bloco.rs` já está no teto (1266) — pede módulo novo + um chamado lá, lote próprio. `flex-aspect-ratio-resize-001` está CORRETO: confirmado com o mesmo harness que, depois do `<script>` mudar a largura do contentor de 500 para 200, a geometria final bate 200×200 exato contra a referência — falha no `claude-raster` só porque ele não corre `<script>` nenhum (mede o estado ANTES da mutação); não é defeito do motor. `inline-flex-column-image-load` tem defeito confirmado e não corrigido: `img.src = "data:..."` atribuído por JS a um `<img>` já no DOM não decodifica nem relayouta nada (harness confirma 0×0 antes E depois do script) — só o `loadResources` do carregamento inicial da página liga uma imagem às dimensões; um `src` setter reativo é funcionalidade própria, fora de âmbito. `cargo test -p rts-dom --lib --features metrics`: 967→**969**, 0 falhas (2 testes novos); `cargo test -p rts-dom-bridge`: 4, sem mudança. **WPT flexbox 375 → 376/489** (+1 por nome: `flex-svg-no-intrinsic-column-001`; zero perdidos, zero encravados — medido duas vezes, a 1ª contaminada por um binário `target/release/examples/claude-raster.exe` ficado de uma sessão anterior, descartada e re-medida do zero). Corpus/paridade/suite `*.test.ts`: medidos no fecho conjunto vaga8-flex (05/09, release, os 5 lotes juntos sobre o main): corpus 141/145, **WPT flexbox 373 → 384/489** (+11 por nome, 0 perdidos), suite 859/888 sem perdidos, 992 testes, paridade 6 páginas 0 movidos, cover 45/57. | `feat/dom-lote-flex-aspect-ratio-collapse` → `74af84133` (2026-09-05) | `aspect_ratio_replaced_corpus.rs` (2); `inline_box::tests::imagem::imagem_sem_dimensao_nenhuma_continua_sem_caixa` (regressão apanhada e corrigida antes do commit) |
| flex-abspos-static-bfc | a STATIC POSITION de um `position:absolute` sem inset num eixo (CSS 2.1 §10.3.7/§10.6.4, Flexbox §4.1); overflow de `<body>`/`<html>` propagado à viewport não estabelece BFC | 8 | ☑ causa 1: `layout/posicao_estatica.rs` (módulo novo) despacha pelo `display` do PAI de `id` — contentor de bloco normal usa o rect do PRÓXIMO irmão em fluxo já layoutado (ele ocupa o lugar que o fora-de-fluxo deixou vago; sem seguinte, o fim do anterior; sem nenhum, o topo do content — `caixa_contentora::content_box`, novo, um passo mais fundo que `padding_box`); contentor flex alinha um item hipotético de tamanho ZERO por `justify-content`/`align-self`, física e já resolvida contra `row-reverse`/`column-reverse` pelo `coluna::fisico_para_eixo` que `flex-justify-logico` já tinha. ACHADO ao medir `align-items-006`: a procura do irmão parava no primeiro nó de TEXTO entre as tags (o espaço em branco comum) — `find` já tinha comprometido esse candidato sem geometria própria e caía sempre no fallback; trocado por `find_map`, que continua até um irmão com `flow_rects` de verdade. Causa 2: `establishes_block_formatting_context` (`bloco.rs`) já suprimia o colapso de margem pai→primeiro-filho quando `overflow≠visible` (CSS 2.1 §8.3.1/§9.4.1) — o que faltava é a excepção do CSS Overflow 3 §3.3: com `<html>` no overflow inicial (visível, o caso comum), o `overflow` do `<body>` PROPAGA para a viewport e o valor USADO no próprio `<body>` é `visible` — não deve contar para o BFC. Sem isto, `body{overflow:hidden}` (idioma comum para esconder um scroll indesejado, nada a ver com o box model) suprimia o colapso body↔primeiro-filho e deslocava a página inteira 8px contra o Blink; `layout/overflow_viewport.rs` (módulo novo, `bloco.rs` não cresce — a chamada substitui `overflow_bfc` na mesma linha). ACHADO à parte: `flex-abspos-inset-nested-{001,002}` falhavam por um `<img>` a 0×0 — não é bug do motor, é o `git sparse-checkout` local do WPT só trazer `css/css-flexbox/support/`, e os dois testes pedirem `../support/1x1-green.png` de `css/support/` (pasta PARTILHADA, fora do que foi trazido); baixado de upstream, os dois JÁ PASSAVAM no código de main sem mudança nenhuma — o motor já sabia transferir o aspect-ratio de um `<img>` esticado num flex (lote `imagens-no-raster`). NÃO fechado: `flex-item-position-relative-001` — um `position:relative` pinta EM LINHA durante a descida do fluxo normal (`layout/relativo.rs`, chamado de `bloco.rs:1192`), mas TODO `position:absolute/fixed` do documento pinta numa passada GLOBAL sempre DEPOIS de todo o fluxo (`layout.rs:309`+seg., `layout/empilhamento.rs`) — o CSS 2.1 Apêndice E pinta os dois juntos, por ORDEM DO DOCUMENTO, e aqui é sempre o oposto; confirmado com `claude-paint-dump` (um decoy `position:absolute` sem inset, ANTERIOR na árvore, cai por cima do quadrado verde de um flex item `position:relative` POSTERIOR — devia ser o contrário). Corrigir pede tratar `position:relative` como "positioned" na mesma passada adiada do `absolute`/`fixed`, e layout+pintura estão FUNDIDOS numa só passada nesta engine — maior do que as duas causas deste lote, fica registado para lote próprio. `cargo test -p rts-dom --lib --features metrics`: 967→**977**, 0 falhas (10 testes novos); `cargo test -p rts-dom-bridge`: 4, sem mudança. **Régua do lote (7 nomeados): 0/7 → 6/7** (só `flex-item-position-relative-001` fica); `flex-abspos-inset-nested-{001,002}` não contam (achado do PNG, fecham por outro lote). Scan próprio (debug, `scripts/claude-lote-wpt-full.mjs`, FORA do build central): **WPT flexbox 375 → 381/489** (+6 por nome: os 4 nomeados mais `flex-aspect-ratio-img-row-014` e `flexbox-paint-ordering-003`, os dois por posição estática em contentor flex; zero perdidos). Corpus CSS/suite `*.test.ts`/paridade: medidos no fecho conjunto vaga8-flex (05/09, release, os 5 lotes juntos sobre o main): corpus 141/145, **WPT flexbox 373 → 384/489** (+11 por nome, 0 perdidos), suite 859/888 sem perdidos, 992 testes, paridade 6 páginas 0 movidos, cover 45/57 pelo coordenador (release) | `feat/dom-lote-flex-abspos-static-bfc` → `625bf39b6` (2026-09-05) | `posicao_estatica.rs` (2 puros) + `posicao_estatica_corpus.rs` (4), `overflow_viewport.rs` (3), `caixa_contentora.rs` (+1) |
| medidor-ahem | a fonte Ahem (WPT: 5 487 testes em `css/`) medida EXATA no `ApproxMeasurer` em vez da aproximação proporcional/mono — sem `@font-face`, sem parser de TTF, só o NOME `font-family: Ahem` (`style::ahem::is_ahem_family`, mesma regra de `is_mono_family`) | 8 | ☑ **ronda 1**: `style::ahem` (nova) + 4 métodos `_family` ADITIVOS no trait `TextMeasurer` (`layout/medidor_texto.rs`, extraído de `medida.rs` — já estava no teto de 500 — porque os métodos novos não cabiam sem o passar), com default que delega no antigo (não arrasta o `EguiMeasurer` de `rts-egui`, que mede pela galley real). Ligado a `intrinsic_content_width` (shrink-to-fit), `inline_box::altura_da_linha` (`line-height:normal`) e `flex_pseudo` — exatamente o gap que `flexbox_flex-natural-mixed-basis-auto` (lote `flex-basis-content-wrap`) já apontava: "a referência usa Ahem e o `ApproxMeasurer` não o emula". `css-flexbox` 475→476/870 (+1, zero perdidos), `css-text` 733/1388 antes E depois (sem mudança — os caminhos ligados não decidem a maioria das fixtures desse corpus). **ronda 2** (pedida pelo coordenador: "a Ahem está ligada onde não decide nada" — o alvo é o WRAP real e a BASELINE): `quebra::wrap_runs` ganha `ahem: bool` + um `medir` local (ponto ÚNICO de medição do ficheiro) que responde pela aritmética exata em vez de pedir ao medidor quando `ahem`; o mesmo parâmetro desce a `hifen.rs` (`emitir_com_hifen`/`maior_prefixo_que_cabe`), `inline_box::prefixo_que_cabe` (a busca binária de quebra dentro de palavra) e `segmento::aplicar_elipse{,_forcada}`/`tabulacao::aplicar_line_clamp`. A BASELINE (`alinhamento_vertical::ascent_com_baseline_propria`/`envelope_com_baseline`/`topo_do_item_com_baseline`) ganha `family: Option<&str>` e chama `font_ascent_family`/`font_descent_family`/`line_height_family` em vez das versões sem família — `linha_ib::ascent_do_item` e `inline_fragmentos::fragmento_do_dono` (baseline de um `<span>`/inline-block) passam a família do PRÓPRIO nó. Nenhum ficheiro cresceu acima do teto: `quebra.rs` e `linha.rs` ficaram nos MESMOS 498 linhas de antes (a consolidação `family`/`mono`/`ahem` numa variável só em `linha.rs` compensou as linhas novas); `altura_do_conteudo` (meia-entrelinha) e o texto solto direto em `child_outer_width`/`child_outer_height` (sem `parent_css` na assinatura) ficam SEM wiring — exigiriam tocar `bloco.rs` (1264, no teto explícito de "não crescer") ou mudar uma assinatura usada por 7 chamadores; parados e ditos, não escondidos. `css-flexbox` (por nome, contra `wpt-baseline-main-870.json`): 477/870, **zero perdidos** contra essa régua — um resíduo achado e explicado, e é PROGRESSO e não regressão: `flexbox_flex-natural-mixed-basis-auto` falhava no main a **2,51%** antes da ronda 1; a ronda 1 levou-o a 0% (passa); a ronda 2 deixa-o a **0,05% — uma faixa de ~1px** (plausível de um nó de texto só-espaço entre `<span>` irmãos, agora medido exato em vez de aproximado). De 2,51% para 1px é a mesma direção que o resto do lote, só que ainda não fechado — não é perda contra o main (que estava em 2,51%), só contra o relatório intermédio da própria ronda 1. `css-text`: **733→754/1388** (28 ganhos, 7 perdidos, líquido +21 — a régua que a ronda 2 visava mover, e moveu). **Os 7 perdidos, investigados por pedido do coordenador** (hipótese testada: "um reftest cujos dois lados dependem da MESMA feature em falta passa por convergência trivial; quando a feature chega, o que se via por baixo era o defeito real" — já tinha acontecido duas vezes na sessão, com `<![CDATA[]]>` e com `writing-mode`), cada um rasterizado nos DOIS lados com o binário do `main` (`origin/main` em `97470ccf0`, sem nenhuma das duas rondas) e com o desta ronda: `text-indent/anonymous-flex-item-001` e `text-indent/anonymous-grid-item-001` (mesma referência, `anonymous-flex-grid-item-001-ref.html`) — **SÓ A REFERÊNCIA muda** (0 px no teste, 660 px na referência): a referência é um bloco comum que agora quebra a linha no sítio EXATO da Ahem; o teste usa `display:flex` (o texto vira item de flex anónimo, um caminho que este lote não tocou) e continua a quebrar onde sempre quebrou — convergência trivial a desfazer-se, aceite, o gap real é o wrap de um item de flex anónimo divergir do de um bloco comum, sem fixture própria ainda. `hanging-punctuation/hanging-punctuation-last` e `-last-rtl` — **os DOIS lados mudam pelo MESMO número** (594 px em cada, o mesmo delta): `hanging-punctuation` não está implementado (busca no crate inteiro, zero ocorrências) — o teste declara todas as caixas a `4em` e a referência varia `4em`/`5em` DE PROPÓSITO para *simular* visualmente o hang; o passe anterior era a aproximação de quebra a pousar por acaso perto do truque de largura da referência, e a medição exata desfez esse acaso nos dois lados por igual — não uma regressão de comportamento correto — a convergência anterior nunca dependeu de `hanging-punctuation` estar implementado, só de duas aproximações a acertar por acaso. `white-space/eol-spaces-bidi-003`, `white-space/white-space-pre-wrap-trailing-spaces-012` e `-015` — os DOIS lados mudam, por números DIFERENTES: as três partilham `width: 4ch`/`5ch` num contentor Ahem, e `Dimension::Ch` (`style/values/dimensao.rs`) resolve `ch` por `MONO_ADVANCE` (0,5498) **sempre**, nunca pela Ahem exata (1,0) — confirmado lendo as DUAS páginas de cada par, a referência declara o MESMO `ch` (`eol-spaces-bidi-003-ref.html`: `width:4ch` idêntico). Um bug REAL e distinto do wrap, pré-existente, que a aproximação global escondia (as duas medições erradas — `ch` e o wrap por PROP_ADVANCE — coincidiam por acaso); a wrap agora exata deixou de coincidir com um `ch` que continua errado. **ronda 3** (pedida pelo coordenador para fechar exatamente estes 3): `Dimension::resolve_family` (novo, `style/values/dimensao.rs`) — aditivo, o MESMO padrão de `text_width_family`: default que delega em `resolve`, só responde diferente para `Ch` quando a família é Ahem (`N×font-size` exato, porque em Ahem `1ch=1em` por construção — o "0" que define `ch` é um glifo igual a todos os outros). NÃO um campo novo em `ResolveCtx` (~50 sítios de construção neste crate, `bloco.rs` incluído) — só o ÚNICO sítio que resolve `width` de um bloco comum (`bloco.rs:494`, uma troca de método na MESMA linha, `bloco.rs` fica no mesmo número de linhas). `css-flexbox`: 477/870, zero perdidos (inalterado — a régua não tem fixture com `ch`+Ahem). `css-text`: **754→759/1388** (5 ganhos, zero perdidos) — fecham os 3 visados (`eol-spaces-bidi-003`, `white-space-pre-wrap-trailing-spaces-012/015`) MAIS 2 de bónus com a mesma causa (`line-break-anywhere-005`, `word-break-break-all-017`, confirmados por leitura a usar `width:4ch`+Ahem também). `cargo test -p rts-dom --lib`: 998→1015 ao todo nas três rondas (17 testes novos: 4 `style::ahem`, 8 `layout::tests::ahem_corpus` — 2 do wrap, 2 do `ch` —, 3 `alinhamento_vertical::tests`), 0 falhas; `cargo test -p rts-dom-bridge`: 4, sem mudança. Achado à parte, investigado e descartado: um "não rasterizou" novo em `white-space/break-spaces-before-first-ideographic-char-001` na corrida completa de `css-text` — reproduzido ISOLADO (o mesmo ficheiro, sozinho) deu a MESMA falha normal de antes (0,98%, sem crash nem hang), então é contenção de recursos de correr várias réguas pesadas em paralelo na mesma máquina, não uma regressão do código. NÃO feito: `child_outer_width`/`altura_do_conteudo` (dito acima); a pintura do glifo Ahem como bloco sólido (`claude-raster.rs` salta `Text` de propósito, dito na ronda 1) | `feat/dom-lote-medidor-ahem` → `cbc6b27b4` (ronda 1), `feat/dom-lote-medidor-ahem-2` → `b41e2817d` (ronda 2) + `4ab96214c` (ronda 3, fecho) (2026-09-05) | `style::ahem` (4), `layout::tests::ahem_corpus` (8), `alinhamento_vertical::tests` (+3) |
| V–Y | a superfície DOM que as bibliotecas pedem | 4 | ☐ | — | §6 |
| dom-ciclo-de-vida-scripts | `loadDocument` (navegação), `<script>` corre ao ligar por `appendChild`/`insertBefore`, `DOMContentLoaded`/`load`, `document.readyState` | 7 | ☑ `loadDocument(html,url)` novo (`lifecycle.ts`): parse→loadResources→scripts do parse em ordem→`readyState="interactive"`+`DOMContentLoaded`→drena a fila de tarefas do documento→`readyState="complete"`+`load` na window. Gancho `__afterConnect` em `appendChild`/`insertBefore` (dom.ts, 1 linha cada): com scripting ligado, um `<script>` que liga corre — inline síncrono sem `load`; com `src` (só `data:` — `http(s)` fica fora, dito) agendado na fila do `DomTimers` (a mesma de `setTimeout`), dispara `load` no elemento SEM bubbling (medido: com bubbling um `<script>` filho de `<body>` disparava também o `load` da window). Duas flags novas no `Dom` (Rust, `dom/ciclo.rs`): `scripting_enabled` (falso por omissão — `parseDocument` nunca liga) e `ready_state`. `Element.src` (getter/setter refletido) fechado de passagem: faltava por completo, `el.src=...` gravava uma propriedade solta no wrapper e nunca o atributo. NÃO feito, dito: `http(s)` no `<script src>` ligado por mutação (ficaria a bloquear quem anexou), evento `error` de um `src` que falha, e a régua de corpus/suite CSS (medida no fecho pelo coordenador) | `feat/dom-ciclo-de-vida-scripts` → `ee2ca4b13`+`034aabe39`+`dcaddaff6`+`e196cb924`(fecho) (2026-09-05) | `tests/dom/claude-pagina-eventos.{html,ts}` (byte a byte com o Edge headless: `head-inline,body-inline,dinamico-inline,apos-append,apos-append-externo,DOMContentLoaded,externo-data,externo-load,window-load`; segundo caso prova `parseDocument` inerte); `cargo test -p rts-dom-bridge` (7/7); `cargo test -p rts-dom --lib --features metrics` (943/943, 1 ignorado). Fecho: corpus 141/145, WPT flexbox 373/489 (por nome: 0 perdidos, 0 ganhos), suite 859/888 sem perdidos, 967 testes, paridade 6 páginas 0 movidos, cover 45/57; a fixture em release (`run_fixture`) dá a ordem do Edge byte a byte e `parseDocument` inerte; a app do comparativo por `runScriptsAt` continua a correr os 3 scripts; zero rondas de retrabalho |

**Estado após a vaga 1 (2026-09-04, 01:41)** — medido com o `rts.exe`
construído sobre `feat/dom-vaga-1` e comparado POR FICHEIRO contra o binário
de 2026-09-03 (`target/baseline.exe`, lista em `base-suite.txt`):

| régua | antes (baseline) | depois | perdidos |
|---|---|---|---|
| corpus CSS (`claude-css-runner`) | 41/49, 23 desvios | **49/49, 0 desvios** | nenhum |
| suite `*.test.ts` por `medir.sh` | 855/884 | **858/887** (+3 fixtures novas) | **nenhum** |
| `cargo test -p rts-dom --lib` | 718 | **756**, 0 falhas | — |
| `cargo test -p rts-dom-bridge` | nunca corria (doctest partido) | **4 + doctests**, 0 falhas | — |

O binário desta medição fica como o próximo `target/baseline.exe` e
`vaga1-suite.txt` como o próximo `base-suite.txt`. O que a vaga 1 NÃO mediu:
o scroll numa janela real (G) — a fixture headless passa, o exemplo em janela
fica para quem tiver ecrã.

**Estado após a vaga 2 (2026-09-04, ~10:00)** — lotes I, J, L, M (K e N já
estavam), medido com o `rts.exe` de `feat/dom-vaga-2` contra o binário do
lote K (`target/baseline.exe`, `base-suite.txt` = `lotek-suite.txt`):

| régua | antes (baseline) | depois | perdidos |
|---|---|---|---|
| corpus CSS | 49/49 | **51/54** — +5 fixtures: `revert`×2 passam, `ua`×3 falham de propósito (15 desvios = lacunas do motor) | nenhum |
| suite `*.test.ts` por `medir.sh` | 858/887 | **859/888** (+`claude-dom-node-lifecycle`) | **nenhum** |
| `cargo test -p rts-dom --lib --features metrics` | 758 (feature partida) | **788**, 0 falhas; a feature `metrics` voltou a compilar | — |
| paridade em 6 páginas locais (só o lote L) | — | desvio máx. 6,1e-5 px | — |

Régua nova: `scripts/css_fixtures_medir_edge.mjs` (Blink headless por CDP;
validado com desvio 0 contra os 49 esperados do Chrome). Encontrado de
caminho: `WeakRef`/`WeakMap` retêm fortemente (#2636), o que bloqueia os
wrappers fracos do lote M. O binário desta medição é o próximo
`target/baseline.exe`; `vaga2-suite.txt` o próximo `base-suite.txt`.

**Estado após a vaga 3 (2026-09-04, ~13:30)** — lotes O, P, R, S-texto + o
lote de réguas da vaga 4, medido com o `rts.exe` de `feat/dom-vaga-3` contra
o binário da vaga 2 (`target/baseline.exe`, `base-suite.txt` = `vaga2-suite.txt`):

| régua | antes (baseline) | depois | perdidos |
|---|---|---|---|
| corpus CSS | 51/54 | **72/86** — +32 fixtures: 21 passam, 11 esperadas a falhar (`tests/css/esperado-a-falhar.txt`: 3 UA, `grid-auto-fill`, `sel-has`, 9 réguas da vaga 4 medidas ANTES do código) | nenhum |
| suite `*.test.ts` por `medir.sh` | 859/888 | **859/888** | **nenhum** |
| `cargo test -p rts-dom --lib --features metrics` | 788 | **844**, 0 falhas | — |
| check local do `dom-rulers` | — | inesperadas: nenhuma; da lista a passar: nenhuma | — |

Retrabalho: 16 rondas em 4 lotes (4,0 por lote; o lote S sozinho 8) — 10
delas por fixtures medidas DEPOIS do código. É a razão da regra nova do §1
("a régua antes do código"), já aplicada às 12 réguas da vaga 4. O binário
desta medição é o próximo `target/baseline.exe`; `vaga3-suite.txt` o próximo
`base-suite.txt`. A paridade das 6 páginas locais foi REGENERADA como "antes"
(a margem de UA e o rect em linha moveram-na, e era isso que devia).

**Estado após a vaga 4 (2026-09-04, ~14:30)** — lotes S-inline, S-transform,
S-decor e o resto de R, todos com a RÉGUA MEDIDA ANTES do código; medido com
o `rts.exe` de `feat/dom-vaga-4` contra o binário da vaga 3:

| régua | antes (baseline) | depois | perdidos |
|---|---|---|---|
| corpus CSS | 72/86 | **82/86** — as 4 que falham são as 3 de UA e `cursor` com `url()` (lista `esperado-a-falhar.txt` encurtada de 14 para 4) | nenhum |
| suite `*.test.ts` por `medir.sh` | 859/888 | **859/888** | **nenhum** |
| `cargo test -p rts-dom --lib --features metrics` | 844 | **868**, 0 falhas (e 861 SEM a feature: o teste dos at-rules ignorados passou a ser condicionado — o `dom-tests` do CI corre sem ela) | — |
| check local do `dom-rulers` | — | inesperadas: nenhuma | — |

**Retrabalho: 0 rondas em 4 lotes** (a vaga 3 tinha custado 16 em 4). A
diferença foi uma só: as fixtures medidas e commitadas antes, e o agente a
correr o teste do corpus no worktree dele. O binário desta medição é o
próximo `target/baseline.exe`; `vaga4-suite.txt` o próximo `base-suite.txt`.

**Estado após a vaga 5 (2026-09-04, ~13:00)** — a régua de PINTURA e o
primeiro lote medido por ela (U-pintura-1), contra o binário da vaga 4:

| régua | antes (baseline) | depois | perdidos |
|---|---|---|---|
| corpus CSS (layout + computed) | 82/86 | **82/86** (as 4 esperadas) | nenhum |
| **pintura** (novo: pixels vs Edge, texto mascarado) | 79/86 ≤ 0,5 %, 4 > 2 % | **84/86 ≤ 0,5 %, 86/86 ≤ 2 %, 0 > 2 %** | nenhuma piorou |
| suite `*.test.ts` por `medir.sh` | 859/888 | **859/888** | **nenhum** |
| `cargo test -p rts-dom --lib --features metrics` | 868 | **870**, 0 falhas; `rts-egui` compila com `PushTransform` | — |

Retrabalho: 0 rondas em 2 lotes. O binário desta medição é o próximo
`target/baseline.exe`; `vaga5-suite.txt` o próximo `base-suite.txt`. As duas
fixtures entre 0,5 % e 2 % na pintura (`object-fit` 1,95 %, `triangulo-de-borda`
1,58 %) são o próximo alvo de pintura; os PNG continuam sem versão.

**Estado após as réguas da vaga 6 (2026-09-04, ~14:00)** — nenhum código do
motor mudou; o que mudou foi o denominador, de propósito:

| régua | antes | depois |
|---|---|---|
| corpus CSS (layout + computed) | 82/86, 4 esperadas | **83/90**, 7 esperadas (3 novas, "por implementar") |
| pintura (novas) | — | `border-juncao` 0,13 %, `hyphens-manual` 0,3 %, `inline-fragmentos` 0,56 %, `font-unidades` 2,45 % |

Os lotes de código que estas réguas pedem, por ordem de custo: **S-hifen**
(`&shy;` como oportunidade de quebra + hífen pintado; `hyphens: none` a
ignorá-lo), **U-pintura-2** (bordas como trapézios nas junções — fecha o
triângulo; e o `claude-raster` a mascarar `DisplayItem::Image` como mascara
texto, para o `object-fit` medir o que o motor faz e não o que o exemplo não
tem), **S-inline-2** (inline com conteúdo em fragmentos de linha: hoje o span
promovido a caixa nem sequer QUEBRA — `cx1` fica 22px alto em vez de 60 — e
engrossa a linha com a borda), **T** (`ch`/`ex`/`line-height: normal` pela
métrica da fonte do `TextMeasurer`; a lista de `font-family` serializada
inteira). Cada brief leva os números acima.

**Estado após a vaga 6 (2026-09-04, ~17:30)** — réguas antes do código, 5
lotes feitos à mão (os 15 agentes da sessão já tinham sido usados), ZERO
rondas de retrabalho:

| régua | antes (vaga 5) | depois |
|---|---|---|
| corpus CSS (layout + computed) | 82/86, 4 esperadas | **86/90**, 4 esperadas (3 UA, `cursor`) |
| pintura (pixels vs Edge) | 84/86 ≤ 0,5 % | **89/90 ≤ 0,5 %** (`object-fit` 1,22 % = a imagem `data:` sem loader) |
| suite `*.test.ts` por `medir.sh` | 859/888 | **859/888**, nenhum perdido em nenhum lote |
| `cargo test -p rts-dom --lib --features metrics` | 870 | **878** |

Lotes: réguas-6 (#2643), S-hifen (#2644), U-pintura-2 (#2645), img-fundo
(#2646), S-inline-2 (#2647), T. As três fixtures medidas antes do código
passaram todas. O que fica: o loader de imagem `data:` para
`<img>`/`list-style-image` (fecha `object-fit`), a fonte REAL de §5.T
(decisão de crate), e as 3 da folha de UA que só uma fonte real fecha.

**O lote V-img, desenhado às 18:30 e FEITO às 21:00 (a decisão mudou no
caminho: os pixels ficam no DOCUMENTO por `set_pixel_data`, não num handle de
Buffer — ver a linha V-img em cima; o que segue é o desenho original):** o `claude-object-fit` (1,22 % na pintura) e o `list-style-image`
param no mesmo sítio, e a investigação disse porquê: **`rts:imgdec` e
`dom.setImage` NÃO existem no motor novo** — `examples/claude-browser.ts` e
`claude-wa-app.ts` chamam-nos, mas são código do motor antigo (nenhum crate
regista o namespace; `setImage` não está em `dom_members()`). Só
`Dom::set_image`/`image_of` (rts-dom) e o `DisplayItem::Image` existem. O lote
tem três peças e uma decisão, tomada aqui para não ser retomada duas vezes:

1. **Descodificador**: crate `png` (e `jpeg-decoder`) em `rts-std`, sob um
   namespace `rts:imgdec` com `decode(ptr,len) -> handle`, `width`, `height`
   (o `#[rtse::class]` — RULE 0b `add-builtin-class`). O `rts-dom` continua
   sem dependências; o `claude-raster` continua a mascarar imagens — a régua
   de pintura das imagens passa a ser a do egui (screenshot da janela), não a
   do exemplo headless.
2. **Bridge**: `setImage(doc, node, handle, off, w, h)` em `dom_members()`
   (o teste de contrato obriga), a chamar `Dom::set_image`; e `imageOf` para
   o `naturalWidth`/`naturalHeight` de `HTMLImageElement`.
3. **Loader no documento** (`dom.ts`, não nos exemplos): ao montar/mutar um
   `<img src>` — `data:` descodificado por `atob` na hora; `http(s)` pelo
   `fetchBytes` assíncrono já existente; e o `list-style-image`/`background-image`
   do CSS pelo mesmo caminho, com a URL resolvida contra a base do documento
   (o que também fecha `claude-cursor-pointer-events`, que é só resolução de
   URL).

Régua ANTES do código: `claude-object-fit` (já medida: as quatro caixas 100×50
com a imagem `data:` de 1×1 por baixo), `claude-list-style-image` (já medida)
e uma fixture nova `claude-img-natural` com um PNG `data:` de 4×2 sem
`width`/`height` (a caixa tem de vir do tamanho natural: Blink 4×2) — a medir
no Edge antes de abrir o lote.

**Se retomou depois de uma paragem:** (1) `git branch -a | grep feat/dom-`
diz que lotes têm branch; (2) `git log main..origin/<branch>` diz se o commit
do lote existe; (3) a tabela diz se foi integrado. Um lote com branch e sem
"☑" é para verificar com o §2 e integrar, não para refazer.

---

## 1. As regras do trabalho (o que não se negocia)

- **RULE 0** do `CLAUDE.md`: antes de editar, ler `docs/ui/html-engine/README.md`
  e a secção 3 do roadmap (os invariantes — dois deles já expiraram e a
  auditoria diz quais), e o relatório da lente que cobre o lote.
- **Tecto de 500 linhas.** Estes já estão acima e **não crescem**: `dom.ts`
  (1 847), `style/syntax.rs` (1 122), `layout/bloco.rs` (1 061),
  `style/scenarios.rs` (829), `style/parse/mod.rs` (724),
  `style/stylesheet/sheet.rs` (580), `layout/vertical.rs` (556),
  `dom/mutacao.rs` (522), `layout/fragmento.rs` (509). Lógica nova = módulo
  novo pequeno; nos grandes entram chamadas.
- **Uma máquina.** Os agentes implementam e commitam na sua branch, num
  worktree isolado, e **não constroem** (um `cargo check -p rts-dom` no fim é o
  máximo — o crate não tem dependências). Quem orquestra constrói UMA vez com
  todos os lotes da vaga, corre as réguas do §2, e devolve o erro exacto ao
  agente do lote. Ver a memória `feedback_uma_maquina_build_centralizado`.
- **A régua é o Chrome.** Nunca se edita uma fixture nem um `.esperado.json`
  para um número subir; uma fixture que falha fica a falhar. Um lote de layout
  entra com um teste Rust que parseia o HTML EXACTO da fixture e afirma os
  rects do Chrome — assim a suite unitária pina o mesmo que o corpus.
- **A régua ANTES do código** (2026-09-04, depois de a vaga 3 custar 14 rondas
  de retrabalho, 10 delas por fixtures medidas depois de implementar): um lote
  começa por um "lote de réguas" — as fixtures são escritas, revistas (sem
  texto onde a pergunta é geometria; ids só no que se afirma; o selector ou a
  propriedade como única coisa em jogo), MEDIDAS no Blink
  (`scripts/css_fixtures_medir_edge.mjs`) e commitadas com `.esperado.json`
  antes de qualquer agente implementar. O brief do agente traz os números; o
  teste Rust em `layout/tests/*_corpus.rs` nasce com os rects do Blink e o
  agente corre-o sozinho (`cargo test -p rts-dom --lib <nome>` é permitido: o
  crate não tem dependências). O vermelho→verde acontece no worktree do
  agente, não na ronda de build do orquestrador.
- **Comparação por ficheiro.** Uma vaga só integra com a lista de PERDIDOS
  vazia nas três réguas (corpus, testes do crate, suite). Um número líquido
  não é um resultado.
- **Sem código morto; comentários dizem porquê; testes nomeiam o comportamento;
  fixtures novas têm prefixo `claude-`.** Convenções do `CLAUDE.md`.
- **Um lote, uma branch, um PR, `--squash`.** Lotes da mesma vaga são
  disjuntos em ficheiros por desenho; onde não puderem ser (o `bloco.rs`), o
  plano diz quem evita que região.

---

## 2. As réguas — os comandos exactos

```bash
# 1. testes do crate (sem dependências: segundos)
cargo test -p rts-dom --lib --no-fail-fast
cargo test -p rts-dom-bridge --no-fail-fast          # lote A em diante

# 2. o corpus CSS (precisa do binário; release, porque o runner é o rts:dom real)
cargo build --release                                 # o pacote RAIZ `rts` é o rts.exe; `-p rts-cli` só constrói a lib
target/release/rts.exe run examples/claude-css-runner.ts   # "N/49 passam" + desvios
CSS_FILTRO=position target/release/rts.exe run examples/claude-css-runner.ts

# 3. a suite, por ficheiro, contra o binário guardado
cp target/release/rts.exe target/baseline.exe         # SÓ no início de uma vaga
bash medir.sh target/baseline.exe base-suite.txt
bash medir.sh target/release/rts.exe now-suite.txt
comm -23 <(awk '$2==1{print $1}' base-suite.txt | sort) <(awk '$2==1{print $1}' now-suite.txt | sort)
#   ↑ a lista de PERDIDOS: tem de vir vazia

# 4. o dump de paridade (só para refactors que devem mover ZERO pixels)
#    docs/ui/modularizacao-rts-dom.md, secção "A régua que valida um refactor"
```

O corpus lê-se por fixture: `passa → passa`, `falha → passa` (ganho),
`passa → falha` (PERDIDO, bloqueia), `falha → falha` (compare os desvios: menos
é progresso, mais é regressão dentro de uma fixture que já falhava).

---

## 3. Vaga 1 — o que a auditoria disse que vinha primeiro (em curso)

Seis lotes disjuntos, lançados a 2026-09-04. Cada um tem o brief completo na
sessão que o lançou; o que fica aqui é o suficiente para verificar e para
retomar.

### A — contrato DOM→JS (`rts-dom-bridge`, `dom.ts`, 3 docs)

- **Fecha:** finding 1 da auditoria. `Element.getBoundingClientRect()` chama
  `dom.boundingComponent`, o bridge regista `boundingRect` → `TypeError` em
  qualquer página. `Element.setStyle(slot,val)` chama `dom.setStyle`, que não
  existe.
- **Entrega:** a fachada a chamar o que existe; `setStyle` apagado (sem
  chamadores) ou implementado sobre `dom/estilo.rs` (com); um `#[test]` no
  bridge que extrai todo `dom.<ident>(`/`engine.<ident>(` de `dom.ts`+`window.ts`
  e afirma que cada um é uma chave de `MEMBERS` — a vista gerada mínima, que
  teria apanhado isto; três funções mortas apagadas (`layout_inline_line`,
  `wrap_text`, `fragment_count`); as três docs que mentiam corrigidas
  (invariante 5, `tests/css/README.md`, `dom-metrics.md`).
- **Aceitação:** `cargo test -p rts-dom-bridge` verde e o teste de contrato
  a falhar se se reintroduzir `boundingComponent`; a fixture nova passa.
- **Depois (não neste lote):** a vista gerada a sério — `#[rtse::class]` fora
  do `rts-core` (branch `feat/class-abi-macro-fora-do-core` já compila a
  macro lá; falta a opção `extend` para namespaces em vários ficheiros) e um
  `.d.ts` emitido por `rts emit-types` que cubra `rts:dom`, `rts:egui`,
  `rts:input`.

### B — um medidor activo (`dom/geometria.rs`, módulo novo, `rts-egui`)

- **Fecha:** "duas verdades para a mesma geometria". `bounding_component` mede
  sempre com `ApproxMeasurer`; a janela pinta com `EguiMeasurer`.
- **Entrega:** `layout/medidor_ativo.rs` — thread-local com
  `Option<Rc<dyn TextMeasurer>>`, `registar`/`limpar`/`with_active`; a geometria
  usa o activo se existir; `rts-egui` regista o seu ao pintar; o comentário de
  `geometria.rs:16-18` passa a ser verdade.
- **Aceitação:** três testes (sem registo = aproximado; com medidor falso =
  reflecte-o; limpar = volta); a chave de cache continua a incluir
  `measurer.identity()`.

### C — `position` (`posicionado.rs`, `layout/relativo.rs` novo, `bloco.rs` mínimo)

- **Fecha:** 6 desvios. `relative` desloca a pintura (reusa o shift do
  `transform`, `bloco.rs:1005-1051`); `absolute` estica quando os dois offsets
  de um eixo estão definidos e a dimensão é `auto`; a margem de um filho
  colapsa através de um pai `relative` sem borda (`#meio.y 50`, hoje 70).
- **Aceitação:** `layout/tests/position_corpus.rs` com os rects do Chrome das
  duas fixtures; `claude-position-*` passam no corpus; `tests/colapso.rs`
  continua verde.
- **Depois:** `sticky` (está no enum, é fluxo normal); static position de um
  `absolute` sem offsets num pai inline; `z-index` além do v1.

### D — grid: rows por áreas (`grid.rs`, módulo novo se passar de 500)

- **Fecha:** 3 desvios. `grid-template-rows: 60px 1fr 40px` com altura 400
  não dá 300 à row do meio, ou não a atribui aos itens por área; o rodapé é
  colocado como se ela medisse 0.
- **Aceitação:** `layout/tests/grid_corpus.rs`; `claude-grid-areas` passa;
  os testes de áreas/spans existentes continuam verdes.
- **Depois (lote R do §5):** `grid-row/column-start/end` completos,
  `grid-auto-flow`/`dense`, tracks implícitas, `auto-fill`/`auto-fit`,
  sizing intrínseco, `align-content`/`justify-self`.

### E — BFC como entidade, floats, `clear` (`layout/bfc.rs` novo)

- **Fecha:** findings 1 e 2 da lente de layout, 6 desvios. A entidade
  `BlockFormattingContext` (exclusões por lado, "estabelece BFC?", clearance
  pendente) evolui o parâmetro `exclusoes` de `layout_block` em vez de
  acrescentar parâmetros; `Clear::clears` por lado; o pai só cresce para
  conter floats quando estabelece BFC (`overflow`≠`visible`, `flow-root`,
  flex/grid/tabela, float, absolute/fixed, inline-block, raiz).
- **Aceitação:** `layout/tests/float_corpus.rs` (as duas fixtures + um caso
  `flow-root` marcado como derivado da spec); o comentário "DIVERGÊNCIA
  CONHECIDA" de `vertical.rs:544` desaparece; nenhuma fixture de flex/grid/
  tabela/texto regride.
- **Risco declarado:** mexer na altura de todo o contentor com float move
  páginas reais; a suite por ficheiro e o dump de paridade são o que o apanha.

### F — baseline por átomo, `vertical-align`, `white-space` (`layout/alinhamento_vertical.rs` novo)

- **Fecha:** 8 desvios. Cada átomo/segmento da linha carrega ascent/descent;
  o line box tem strut; os oito valores de `vertical-align` são deslocamentos
  contra a baseline dominante; `pre` abre linha no `\n`; a `line-height` do
  shorthand `font` chega ao line box de um pai misto (bloco + inline).
- **Constantes:** ascent/descent/x-height do `ApproxMeasurer` derivadas do
  esperado do Chrome e documentadas como calibração (o método de
  `style/text_metrics.rs`); o `EguiMeasurer` sobrescreve-as com a métrica real
  do epaint.
- **Aceitação:** `layout/tests/inline_corpus.rs`; as três fixtures passam;
  nenhuma fixture de texto que hoje passa regride.

---

## 4. Vaga 2 — o resto do que a auditoria marcou como estrutural ou dívida

Começa quando a vaga 1 está integrada e medida. G e H podem correr em
paralelo com a vaga 1 se houver agentes livres: não tocam em layout.

### G — o scroll vive no documento (`Dom`, bridge, `dom.ts`/`window.ts`, `rts-egui`)

- **Fecha:** finding 3. O offset de scroll (página e cada `overflow:auto`)
  existe só em `egui::Context::memory()` (`frame/render/mod.rs:213`,
  `scroll.rs:27-30`); `scrollTop` não existe, `scrollTo`/`scrollBy` são vazios.
- **Desenho:** em `Dom`, `scroll: Cell<(f32,f32)>` para a página e
  `scroll_regioes: RefCell<HashMap<NodeIdx,(f32,f32)>>` — o padrão de
  `hovered`/`focused_input`. O bridge ganha `scrollTop`/`scrollLeft`/
  `scrollHeight`/`scrollWidth`/`scrollTo` (por nó e para `window`). O backend
  LÊ para desenhar e traduzir hit-test, e ESCREVE só em resposta a input
  (roda, arrastar barra, teclado) — nunca guarda para si. `scroll.rs:96-103`
  deixa de mutar a `DisplayList` a posteriori: o layout emite o `BeginClip`
  já deslocado porque lê o offset do `Dom`.
- **Aceitação:** teste Rust (mutar o offset muda o rect devolvido por
  `bounding_component` de um filho de `overflow:auto`); fixture
  `claude-dom-scroll.test.ts`; `examples/claude-tarefas.ts` com uma lista mais
  alta que a janela a rolar por `scrollTo` E pela roda.

### H — o escopo de página não vê o Node (`rts-core`, `rts-codegen`, `rts-host`)

- **Decisão TOMADA (2026-09-04, pelo orquestrador):** este motor NÃO é hoje
  uma fronteira de segurança — mesmo processo, mesmo heap, mesma `Context`, e
  `require`/`fetch` de recursos que a própria página nomeia não passam por
  nenhuma política. O código passa a DIZÊ-lo: o comentário de `NODE_ONLY`
  (`rts-codegen/src/emit/globals.rs`) já não promete o que não cumpria. As
  duas fugas fecham como CORRECÇÃO — é o bug que a lista existe para impedir
  desde sempre (uma página que vê `setImmediate` monta o React pelo ramo Node)
  — e não como implementação de uma fronteira de segurança que este motor não
  tem.
- **Feito, branch `feat/dom-escopo-pagina-sem-node`:**
  (1) o nome nunca entra na cadeia que `Scope::lookup` resolve a zero hops:
  `globals::without_node_only` filtra `NODE_ONLY` de `enclosing` ANTES de
  `emit_page_program`/`emit_eval_program` construírem o `chain`/`Scope` —
  opção (a) do enunciado, porque filtrar o resultado fecha a fuga
  independentemente do mecanismo exacto que a produzia, e o mecanismo em si
  não ficou provado por leitura estática sozinha (a auditoria mediu-o AO
  VIVO);
  (2) `Scoped::Eval`/`Scoped::Page` ganharam `hide_node_globals: bool`;
  `rts-host/src/live.rs` decide-o comparando o ambiente a
  `rts_core::entry::global_object` (só `vm.runInThisContext` partilha o
  global real) e, quando `true`, marca o ambiente com
  `rts_core::entry::mark_hides_node_globals` — uma propriedade `__rts_`
  no PRÓPRIO objecto, e não uma tabela lateral por CÉLULA, porque uma célula
  libertada e reutilizada tornaria uma entrada de tabela obsoleta uma marca
  no objecto ERRADO. Um `eval()` de dentro da página lê a marca de volta com
  `rts_core::entry::hides_node_globals`, que anda a MESMA cadeia
  `__rts_outer` que `environment_names` já anda.
- **Aceitação:** fixture `tests/claude-dom-page-nao-ve-process.test.ts`: num
  `<script>` de página, `typeof process`, `typeof Buffer`,
  `typeof setImmediate`, `typeof require` e `eval("typeof process")`
  respondem `"undefined"`; `typeof setTimeout`/`typeof fetch`/
  `typeof document` continuam o que um browser dá; e um controlo prova que
  FORA da página `typeof process` continua `"object"` — a diferença é o
  ESCOPO, não uma remoção do global.
- **Risco aceite e não fechado por este lote:** `vm.runInContext`/
  `runInNewContext` com um sandbox comum (não `runInThisContext`) passam a
  também esconder `NODE_ONLY` do sandbox — inclusive se o chamador tivesse
  posto lá `sandbox.process = algo` explicitamente, esse `process` deixa de
  resolver pela CADEIA (continua a resolver por leitura directa de
  propriedade, `sandbox.process`). Nenhum teste existente pina o caso
  contrário; `crates/rts-node/README.md`/`vm.rs` é onde revisitar se um
  programa real depender disso.
- **Gap conhecido, não fechado:** `eval` INDIRECTO — `(0, eval)("process")`,
  `globalThis.eval("process")`, um nome que aponta para `eval` chamado sem ser
  como identificador nu — continua a ver `process` de dentro de uma página. O
  mecanismo (`rts_core::entry::eval_source`) passa sempre `environment =
  undefined` para o compilador, que é como a especificação diz "corre no
  escopo global" — mas este motor não tem um global por página, então
  `hides_node_globals(undefined)` não tem nada para andar e responde `false`.
  Fechar isto exigiria saber de que ESCOPO A CHAMADA partiu sem um objecto de
  ambiente para perguntar, o que é um problema diferente do que este lote
  respondeu. A fixture só cobre a forma DIRECTA (`eval("...")`), que é a que a
  auditoria mediu e a que o enunciado pediu.
- **Não feito:** um `Context`/heap por documento em vez do singleton
  thread-local — a decisão tomada foi "não é fronteira de segurança", o que
  torna este item fora de escopo em vez de pendente.

### I — a folha de UA em CSS real (`style/`, `block/ua.rs` morre, `scrollbar.rs` morre)

- **Fecha:** dois findings da lente de estilo. `UA_TABLE` (13 slots) +
  `ua_display(tag)` (um `match` chamado pelo layout DEPOIS da cascade) não
  exprimem selectores nem propriedades fora do conjunto; `<th>` não é negrito.
  `scrollbar.rs` é um segundo parser de CSS textual com um bug de aninhamento
  em `@media`.
- **Desenho:** `style/ua.css` (texto, `include_str!`), parseado pelo MESMO
  `style::parse`, anexado ao `Stylesheet` com origem UA (abaixo de qualquer
  layer de autor, e abaixo do `!important` de autor pela ordem da cascade —
  o `!important` de UA fica acima, como a spec diz); `used_display` deixa de
  consultar `ua_display`; `::-webkit-scrollbar*` casa pelo matcher normal e
  `scrollbar.rs` só LÊ o computed.
- **Aceitação:** fixtures medidas no Chrome para `th`, `ul`/`ol` (a de
  `list-style-type` já existe), `input:disabled`, `h1`…`h6`, `table`;
  o dump de paridade (§2.4) — aqui vai mover pixels, e cada um tem de ser
  explicado por uma regra da folha nova.

### J — `DeclarationRecord` na cascade, e `revert`/`revert-layer` (`style/stylesheet/sheet.rs`)

- **Fecha:** a cascade colapsa cascaded+specified em `declarations_from`
  (`sheet.rs:519-577`); a proveniência morre aí.
- **Desenho:** por elemento, por propriedade, a lista ordenada de candidatas
  `{origem, layer, importância, especificidade, ordem, valor declarado}`;
  a redução para `ComputedStyle` é o último passo; `revert` recua uma origem,
  `revert-layer` uma layer. Não é uma segunda lista de regras: é o que
  `declarations_from` devolve antes de reduzir. Cuidado com o custo — a
  memoização por revisão (`computed_memo`) tem de continuar a valer; medir com
  `dom_metrics` antes e depois.
- **Aceitação:** fixtures `claude-revert.html`/`claude-revert-layer.html`
  medidas no Chrome; `getComputedStyle` inalterado nas 49 existentes.

### K — invalidação escopada para `:nth-child` (`dom/invalidacao.rs`, `sheet.rs`)

- **Fecha:** `position_sensitive()` é UM booleano por folha; um
  `tr:nth-child(odd)` em qualquer lado faz cada mutação estrutural cair no
  `touch()` global.
- **Desenho:** invalidar os irmãos (e o pai) do nó mutado quando há regras
  sensíveis a posição — o conjunto de invalidação por selector do Blink, na
  forma mínima.
- **Aceitação:** `dom_metrics`: cascades por `appendChild` numa página de
  3 000 nós com uma regra `:nth-child` deixam de ser O(n).

### L — a cache de fragmentos em flex/grid/tabela (`flex.rs`, `coluna.rs`, `grid.rs`, `table/`)

- **Fecha:** `layout_block_reusing` só é chamado do fluxo de bloco
  (`vertical.rs:464`); flex/grid/tabela/out-of-flow chamam `layout_block`.
- **Desenho:** a `FragmentKey` não depende do display; os três caminhos
  passam a chamar `layout_block_reusing`. O que pode partir: o "forced size"
  que flex/grid impõem tem de entrar na chave.
- **Aceitação:** `dom_metrics` numa app de cards em flex (o
  `claude-tarefas.ts`): subárvores reusadas > 0 por frame de mutação de texto.

### M — ciclo de vida do nó (`dom/arvore.rs`, `mutacao.rs`, `dom.ts`)

- **Fecha:** a arena nunca recicla, `__wrappers` só cresce, `dom.free` nunca
  é chamado pela fachada.
- **Desenho:** freelist de `idx` para nós desligados sem referência externa
  (o `NodeId` versionado já dá a geração); `__wrappers` sobre `WeakRef`/
  `FinalizationRegistry` quando o motor os tiver (`WeakMap` já existe — issue
  #217 diz o que falta); `document.close()`/descarte a chamar `dom.free`.
- **Aceitação:** teste: inserir e remover 100 000 nós não faz a arena crescer
  além do pico vivo.
- **Feito, branch `feat/dom-lote-m-ciclo-de-vida` (2026-09-04):** geração POR
  NÓ (`dom/freelist.rs`) — `Dom::node_generation: Vec<u32>` paralelo a
  `nodes`; `to_abi`/`from_abi` não mudaram (já empacotavam sem assumir
  geração uniforme). `alloc_slot` reusa um `idx` da freelist antes de
  crescer a arena; `recycle` incrementa só a geração DESSE `idx` e purga
  TODO mapa lateral indexado por `NodeIdx` (`style_overrides`, `listeners`,
  `listener_cbs`, `input_values`, `image_pixels`, `own_pixels`,
  `scroll_regioes`, `active_transitions`, `prev_computed`, `anim_override`,
  `anim_start`, `dirty_self`, `dirty_children`, `last_fragment`, `hovered`,
  `focused_input`) — sem isto o PRÓXIMO ocupante do `idx` herdaria estado do
  anterior. Quem decide reciclar: a fachada TS (decisão (a) do enunciado,
  rejeitada a (b) — contagem de referências exigiria uma chamada extra em
  CADA leitura que devolve um `NodeId`, não só na remoção). Novo membro do
  bridge: `dom.releaseSubtree(doc, node)`; a fachada chama-o de
  `removeChild`/`remove()` só quando NENHUM nó da subárvore removida tem
  wrapper em `__wrappers` — `Element.isConnected` novo (getter, sobe por
  `parentNode` até `rootId`), `Document.nodeCount`/`Document.close()`
  (chamam `dom.nodeCount`/`dom.free`+`__dropWindow`, que já existiam sem
  chamador). **`WeakRef`/`FinalizationRegistry` verificados e NÃO usados**:
  `WeakRef.deref()` neste motor nunca devolve `undefined` (o alvo é uma
  propriedade própria comum — `crates/rts-core/.../weakref.rs`) e
  `WeakMap`/`WeakSet` também retêm forte, então nenhuma coleção do lado TS
  hoje esquece uma entrada sozinha; um wrapper só é esquecido pela chamada
  explícita que este lote acrescentou. Testes: `dom/tests/freelist.rs`
  (Rust, 5 testes) + `tests/claude-dom-node-lifecycle.test.ts` (4 cenários).
  `cargo check -p rts-dom -p rts-dom-bridge --tests` limpo (só avisos
  pré-existentes noutros ficheiros). **Não medido ainda**: `cargo test`/a
  suite completa — por integrar pelo orquestrador.

### N — as réguas no CI (`.github/workflows/`, `tests/css/README.md`)

- **Fecha:** nenhum instrumento do `rts-dom` corre em CI.
- **Entrega:** um job (`continue-on-error: true`, como os outros três — está
  escrito no `CLAUDE.md` porquê) que corre `cargo test --profile fast -p
  rts-dom -p rts-dom-bridge` e o corpus, e reescreve o bloco de números do
  `tests/css/README.md` entre marcadores, como o `cross-runtime` faz.
- **Depois:** a primeira régua de PINTURA — screenshot-diff de um subconjunto
  pequeno (cor sólida, gradiente, borda, sombra) contra capturas do Chrome.

---

## 5. Vaga 3 — o CSS como área

O inventário `docs/ui/css-implementation-gaps.md` (secções 4, 5 e 6) é a
lista; aqui está a ordem e a forma. Cada lote começa por uma fixture medida
no Chrome (o procedimento é `scripts/css_fixtures_medir.md`) e só depois pelo
código. Um lote sem fixture não entra.

### O — selectores que faltam (`style/selector/`)

`:has()` (matching relacional + invalidação por descendente — depende de K),
`:target`, `:scope`, `:default`, `:placeholder-shown`, `:autofill`, `:modal`,
`:active`/`:visited` (o DOM tem de conservar o estado), `:focus-visible` real
(distinguir rato de teclado — depende do `rts-input`). Pseudo-elementos que
GERAM caixa: `::marker` (o `listitem/` já emite o marcador — é dar-lhe
estilo), `::first-line`, `::first-letter`, `::placeholder`, `::selection`.

### P — at-rules (`style/stylesheet/`)

`@import` (com o mecanismo de carregamento do lote G/`__readResource`, e a
política que o crítico diz que falta), `@font-face` (depende de T),
`@container` (queries de contentor precisam do tamanho usado → uma segunda
passada de estilo dependente de layout; desenhar antes de codar),
`@property`, `@scope`, `@page`/`@counter-style` (baixa). `@media` completa:
`orientation`, `prefers-*` (com um valor do host), `resolution`, listas e
`not`/`only`.

### Q — CSSOM (`dom/estilo.rs`, `style/stylesheet/sheet.rs`)

A hierarquia que as bibliotecas tocam: `document.styleSheets`, `cssRules`
com índices estáveis, `CSSStyleRule.selectorText`/`.style`, `CSSMediaRule`,
`CSSKeyframesRule`, `CSSStyleDeclaration` com `cssText` canónico,
`getPropertyValue`/`setProperty`/`removeProperty`/`priority`. O
`insert_rule`/`delete_rule` transaccional existente é a base. Depende de J
(especificado vs computado).

### R — grid e flex completos (`layout/grid*.rs`, `layout/flex.rs`, `coluna.rs`)

Grid: colocação por linhas (`grid-row/column-start/end`, spans negativos),
`grid-auto-flow` e `dense`, tracks implícitas (`grid-auto-rows/columns`),
`repeat(auto-fill|auto-fit)`, `minmax` completo, sizing intrínseco
(`min-content`/`max-content`/`fit-content`), `align-content`/`justify-content`
/`justify-self`/`align-self` completos, `subgrid` (baixa). Flex: o piso de
`min-content` no `flex-shrink` (o primitivo existe em `table/widths`),
`flex-direction: *-reverse`, `align-content` em multi-linha, `order` completo,
percentagens de `flex-basis`, `gap` em coluna. Cada um com a sua fixture.

### S — propriedades parseadas sem efeito (a lista da secção 5 do inventário)

Por grupo, e cada grupo é um lote com fixtures:
- **texto:** `text-overflow: ellipsis`, `word-spacing`, `tab-size`,
  `line-clamp`, `hyphens` (precisa de dicionário — decidir), `text-wrap:
  balance/pretty`, `list-style-image`.
- **decoração:** `text-shadow`, `text-decoration-style/color/thickness`,
  `text-underline-offset`, `background-clip/origin/attachment`,
  `mix-blend-mode` (pede composição — ver U).
- **imagens e recortes:** `background-image` (url — precisa do loader e do
  cache de imagens que `image_pixels` já tem), `background-repeat/position/
  size`, `object-fit/position`, `clip-path` (básico: `inset`, `circle`),
  `filter` (`opacity`, `blur` pede compositing).
- **transformações:** `transform-origin`, matriz 2D completa (`skew`,
  `matrix`), depois 3D com `perspective` (pede pintura por camadas — U).
- **host:** `cursor` e `pointer-events` (hit-test tem de os consultar),
  `scrollbar-width/color` (após G), `caption-side`, `zoom`.

### T — fontes reais (`layout/medida.rs`, `rts-egui`, e uma decisão de crate)

`@font-face` com carregamento de `ttf`/`otf`/`woff2` (o epaint carrega
fontes; `woff2` precisa de um decodificador), fallback por família e por
script, métricas reais em todo o lado (após B e F), kerning e ligaduras via
shaping (`rustybuzz` atrás do `TextMeasurer` — o trait já remede prefixos
inteiros, por isso encaixa), quebra de linha UAX#14 (`unicode-linebreak`) e
clusters de grafema (`unicode-segmentation`) em vez de `char_indices`. O
`rts-dom` NÃO ganha dependências por isto: o shaping vive atrás do trait, no
backend, e o headless calibrado continua a ser o modo sem janela. Bidi
(UAX#9) e `writing-mode` vertical só depois — é uma passada entre `wrap_runs`
e o posicionamento, e a lente 5 diz que não verificou se cabe sem mais.

### U — composição e pintura (`layout/pintura.rs`, `display.rs`, `rts-egui`)

Stacking contexts completos (`z-index` real, `opacity` < 1, `transform`,
`isolation`), pintura por camadas quando um segundo backend o pedir,
`box-shadow` com `spread`/`inset` completos, `border-image`, gradientes
radiais e cónicos, `outline`. E a régua de pintura de N, sem a qual nada
disto se mede.

---

## 6. Vaga 4 — a superfície DOM que as bibliotecas pedem

O React 18 e o Preact 10 montam e respondem (2026-08-30). O método que os
fez funcionar foi correr a biblioteca REAL e ler onde parava; é o método
desta vaga. A ordem é por quantas páginas reais cada uma destrava.

### V — o que o React ainda não exerce

`MutationObserver` (o que qualquer framework de "islands" usa),
`IntersectionObserver` e `ResizeObserver` (precisam do layout — após B/G),
`requestAnimationFrame` alinhado ao frame do backend, `getComputedStyle`
completo (valores iniciais resolvidos — a fixture `claude-computed-valor-inicial`
já pina o `""`), `dataset`, `classList` vivo (hoje método-valor),
`insertAdjacentHTML/Element`, `Range`/`Selection` (o caret e a selecção de
texto que o `input.rs` já tem por baixo), `<template>`/`content`,
`DocumentFragment`, `cloneNode(deep)`, `contains`, `closest`, `matches`
(verificar quais já existem em `dom.ts` — parte já existe).

### W — formulários completos

Constraint validation (`checkValidity`, `:valid`/`:invalid`, `required`,
`pattern`), `FormData`, `submit` com serialização, `<select multiple>`,
`<input type=range|date|color|file>` (o `replaced.rs` tem o básico),
`contenteditable` (grande — desenhar antes).

### X — bibliotecas reais como régua

Uma por lote, real do CDN, num exemplo `examples/claude-<lib>-janela.ts` e
numa fixture headless que afirma o DOM resultante: **Vue 3** (usa
`MutationObserver`? não — usa `Proxy` e `queueMicrotask`; deve montar quase
já), **jQuery 3** (o que mais exercita `getComputedStyle`, `offset()`,
`outerWidth` — precisa de B), **htmx** (`fetch` + `insertAdjacentHTML` +
`MutationObserver`), **Alpine.js**, **Lit** (web components: `customElements`,
shadow DOM — é a única desta lista que pede uma árvore nova; adiar).

### Y — Shadow DOM e custom elements

`attachShadow`, `<slot>`, `:host`, `::slotted`, `::part`, `customElements.define`
com os callbacks de ciclo de vida. Uma árvore de composição além da árvore de
nós — é o primeiro item que pede ao `Dom` de 51 campos um tipo separado (a
lente 1 diz onde isso dói). Só depois de I, J e V.

---

## 7. O que este plano não decide

- ~~Se o motor é uma fronteira de segurança (§4.H).~~ Decidido 2026-09-04: não
  é. §4.H tem o que isso mudou no código.
- Se e quando `dom.ts` (1 847 linhas) é partido em ficheiros — o bridge
  concatena preludes; partir é mecânico e vale um lote próprio com o dump de
  paridade a provar zero pixels movidos.
- Se o `#[rtse::class]` chega ao `rts-dom-bridge` antes ou depois da vaga 2 —
  depende da opção `extend` da macro, que é trabalho do `rts-macro`.
