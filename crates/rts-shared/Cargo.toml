[package]
name = "rts-shared"
version = "0.1.0"
edition = "2024"

# rts-shared — camada UNIVERSAL (roda em qualquer alvo, incl. browser/wasm).
# Namespaces pure-compute + globais universais que NÃO tocam backend (I/O, OS,
# rede, async). Depende SÓ do motor (rts-engine) + libs puras.
# ⚠️ NUNCA depender de rts-std (quebraria o alvo browser).
# Ver .claude/plans/partitioned-meandering-milner.md.

[dependencies]
# Um membro pode declarar um EMITTER Cranelift nativo (ver `rts_engine::NativeEmit`).
cranelift-codegen = "0.131"
cranelift-frontend = "0.131"
rts-engine = { path = "../rts-engine" }
# Primordiais movidos p/ rts-primitives (Fase 2): collections/vec.rs chama
# rts_primitives::string::rt::* por símbolo. Aresta acíclica shared → primitives.
rts-primitives = { path = "../rts-primitives" }
regex = "1"
fancy-regex = "0.13"
indexmap = "2.14.0"
time = { version = "0.3", features = ["local-offset"] }
anyhow = "1.0"
unicode-normalization = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0", features = ["preserve_order"] }
json5 = "0.4"
