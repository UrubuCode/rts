//! `node:fs/promises` — the promise-returning fs surface (readFile/writeFile/stat/... as
//! Promises, reachable as `import { promises } from "node:fs"` / `node:fs/promises`).
//! DEFERRED: needs the engine async primitive (see docs/node-implementation/). No symbols
//! yet — this file is the designated slot for them.
